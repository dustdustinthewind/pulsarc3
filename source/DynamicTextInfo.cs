// DynamicTextInfo
using UnityEngine;
using UnityEngine.UI;

public class DynamicTextInfo : MonoBehaviour
{
	public string id = string.Empty;

	private void NEKCPLGFOFD()
	{
	}

	private void DKGBFNCOAEO()
	{
		if (id == " not exist")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "Outdated version. Please restart the game to get lastest updates." + Helpers.GetGameVersion();
		}
	}

	private void POIMNOBDBBN()
	{
	}

	private void JILOMOBDPIA()
	{
		if (id == "_Value4")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "CameraFilterPack/AAA_WaterDrop" + Helpers.GetGameVersion();
		}
	}

	private void FOMNCPKKCFN()
	{
	}

	private void KCDOMIJBFLL()
	{
	}

	private void BEBNOKFLJPH()
	{
		if (id == "_FadeFX")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "SelectorMusicToggle" + Helpers.GetGameVersion();
		}
	}

	private void LJIHHJOAJCN()
	{
	}

	private void BMODOIJGIOO()
	{
	}

	private void CNGAJDBOCLJ()
	{
		if (id == "Commands for challenges")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "CameraFilterPack/Blend2Camera_Saturation" + Helpers.GetGameVersion();
		}
	}

	private void LPMINJJPDCH()
	{
	}

	private void NNFMIAFHMJM()
	{
		if (id == "mapselector.filter.favoriteonly")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "[LobbyPlayerElement] Created lpe for " + Helpers.GetGameVersion();
		}
	}

	private void JDGFCEPDKAJ()
	{
	}

	private void JOACBIEHHCE()
	{
	}

	private void Update()
	{
	}

	private void ILCFPCIPENO()
	{
		if (id == "<b>")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + ":" + Helpers.GetGameVersion();
		}
	}

	private void FANADGBGCPI()
	{
		if (id == "_ColorLevel")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "Failed to Instantiate prefab: " + Helpers.GetGameVersion();
		}
	}

	private void Start()
	{
		if (id == "menu.copyright")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "\n" + Helpers.GetGameVersion();
		}
	}

	private void KLILJHJNICK()
	{
		if (id == "challenges/")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "_Value" + Helpers.GetGameVersion();
		}
	}

	private void IMCKJCHKMKB()
	{
		if (id == "_Noise")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "_Value5" + Helpers.GetGameVersion();
		}
	}

	private void CCLNNLCOPBL()
	{
		if (id == "menu.tabid")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "workshop." + Helpers.GetGameVersion();
		}
	}

	private void HLIAEEMGBHN()
	{
		if (id == "_FixDistance")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "CameraFilterPack/Blend2Camera_Color" + Helpers.GetGameVersion();
		}
	}

	private void AIJGAJIOJDJ()
	{
	}

	private void OCMKCBBCEFG()
	{
	}

	private void FABKIGNFECE()
	{
	}

	private void CGBHOELMAOC()
	{
	}

	private void BMIOFJFMCBG()
	{
		if (id == "_MainTex2")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "saveGameName " + Helpers.GetGameVersion();
		}
	}

	private void DKOPKPBLDHH()
	{
		if (id == "_Value3")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + ".lastCheckpoint.correctScore" + Helpers.GetGameVersion();
		}
	}

	private void KDMANOEMOCA()
	{
		if (id == "_TimeX")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "Aug" + Helpers.GetGameVersion();
		}
	}

	private void BMOFEBKGLFI()
	{
		if (id == "_Value")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "maps." + Helpers.GetGameVersion();
		}
	}

	private void FIKFJDFELIP()
	{
		if (id == "Object ID. Case-Sensitive")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "BadgeText" + Helpers.GetGameVersion();
		}
	}

	private void EPEGAEGDJAM()
	{
		if (id == " Remote called.")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "Tab1Content" + Helpers.GetGameVersion();
		}
	}

	private void DNENFLNCIJP()
	{
	}

	private void HPNNCNNFMGK()
	{
	}

	private void FJHFOBHJEHL()
	{
	}

	private void DLBODOFAJGM()
	{
		if (id == "maxScore")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "UseFinalGlassColor" + Helpers.GetGameVersion();
		}
	}

	private void OIBMHPIFAKK()
	{
	}

	private void HAIAHJPCPAG()
	{
		if (id == "[MapEditor] Loading map: ")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "ExitButton" + Helpers.GetGameVersion();
		}
	}

	private void AEIJFLJEABG()
	{
	}

	private void GPNAOAKCMHC()
	{
		if (id == "_Value2")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "_BorderColor" + Helpers.GetGameVersion();
		}
	}

	private void OEENOOGEEHD()
	{
	}

	private void MFMIODIAKNI()
	{
	}

	private void LCJHDLKJEOM()
	{
	}

	private void CHOPDIGHJNH()
	{
		if (id == "CameraFilterPack/TV_Distorted")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "[MapsData] User's maps was loaded" + Helpers.GetGameVersion();
		}
	}

	private void PMPKMGKAAJH()
	{
		if (id == "#,0")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "The given 2D texture " + Helpers.GetGameVersion();
		}
	}

	private void DNNFHBOOPIN()
	{
		if (id == "DifficultyBG")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "_TimeX" + Helpers.GetGameVersion();
		}
	}

	private void DPIPGGDNGHN()
	{
	}

	private void FBMDHDBELEK()
	{
	}

	private void HMPGIFBJFIK()
	{
		if (id == "_SpawnHeuristic")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "DPADHOR" + Helpers.GetGameVersion();
		}
	}

	private void DAHFFNNIGML()
	{
		if (id == "CameraFilterPack/Blend2Camera_LinearLight")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "[PlayerBase] Ready" + Helpers.GetGameVersion();
		}
	}

	private void BGFJOEPFOPM()
	{
	}

	private void BGDONBMDPGM()
	{
	}

	private void EJFJENFKLND()
	{
	}

	private void JKFDDNMPOJH()
	{
		if (id == "MenuScene")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "offsets" + Helpers.GetGameVersion();
		}
	}

	private void NKLIHNJCHOG()
	{
	}

	private void AGEJKLMJGDO()
	{
		if (id == "[ERROR KEY {0}]")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "UseFinalGlassColor" + Helpers.GetGameVersion();
		}
	}

	private void JHPOIOELNCG()
	{
	}

	private void FNCDAPDOBBI()
	{
		if (id == "end")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + " " + Helpers.GetGameVersion();
		}
	}

	private void EFJDNLGNACH()
	{
		if (id == "inventory.selected.")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "caret" + Helpers.GetGameVersion();
		}
	}

	private void JOPCCCCHNLI()
	{
		if (id == "menu.selectedplaymode")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "Save Game" + Helpers.GetGameVersion();
		}
	}

	private void NBGIMIDICKE()
	{
		if (id == "Connection error: ")
		{
			Text component = GetComponent<Text>();
			component.text = component.text + "/" + Helpers.GetGameVersion();
		}
	}
}
