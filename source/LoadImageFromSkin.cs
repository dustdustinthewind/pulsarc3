// LoadImageFromSkin
using System.Collections;
using UnityEngine;
using UnityEngine.UI;

public class LoadImageFromSkin : MonoBehaviour
{
	public string imageID;

	private bool ABLMLLLDOPO;

	private void GBFPAEDPOPP()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(OGGGNDODCBK());
		}
	}

	private IEnumerator KGJDJKCHMNN()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private void JBBCMAJIMCA()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(FLHPGPHOKAP());
		}
	}

	private void IBEAPBDAOCC()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(NNIPHJLOCAL());
		}
	}

	private IEnumerator DCJLBJBHNDM()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private IEnumerator NMFGPCDBHBJ()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private void EJNKBOLGHJC()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(DCJLBJBHNDM());
		}
	}

	private IEnumerator NNIPHJLOCAL()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private IEnumerator FLHPGPHOKAP()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private void GFLAINNLMBO()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(DMJJDOBMMEK());
		}
	}

	private IEnumerator AOLPHFKPEKI()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private void FDIJLKIIMLL()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(FFCGCCMCGDD());
		}
	}

	private void EMAMCDFBGOA()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(KMBGOLLFLKG());
		}
	}

	private void OnEnable()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(KGJDJKCHMNN());
		}
	}

	private IEnumerator PNOLELPAOBP()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private IEnumerator GEDPMDKLICD()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private void MLLPGPANCHI()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(KGJDJKCHMNN());
		}
	}

	private IEnumerator OOHFLIIOAEN()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private void IBFHEDHJDLH()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(GPCEPAFHDNO());
		}
	}

	private IEnumerator OGGGNDODCBK()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private IEnumerator GPCEPAFHDNO()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private void INGOODALACO()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(FFCGCCMCGDD());
		}
	}

	private IEnumerator DMJJDOBMMEK()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private void IFKNIHJCAFB()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(OOHFLIIOAEN());
		}
	}

	private void IFBKMAEMKNN()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(FLHPGPHOKAP());
		}
	}

	private IEnumerator FFCGCCMCGDD()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private void LNIOFCGDDKP()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(NMFGPCDBHBJ());
		}
	}

	private void OKHGJFFIIBA()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(GPCEPAFHDNO());
		}
	}

	private IEnumerator KMBGOLLFLKG()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private void GLKJFIOCPLF()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(DCJLBJBHNDM());
		}
	}
}
