// LoadImageFromSkin
using System.Collections;
using UnityEngine;
using UnityEngine.UI;

public class LoadImageFromSkin : MonoBehaviour
{
	public string imageID;

	private bool ABLMLLLDOPO;

	private IEnumerator FLHPGPHOKAP()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private void GLKJFIOCPLF()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(HCLAPCLBPKK());
		}
	}

	private void MDMDGKHDPKP()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(AOLPHFKPEKI());
		}
	}

	private IEnumerator DCJLBJBHNDM()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private void OnEnable()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(KGJDJKCHMNN());
		}
	}

	private void JBBCMAJIMCA()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(IHMDPKFAIII());
		}
	}

	private IEnumerator LMPFJHDJPIB()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private void OKHGJFFIIBA()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(LMPFJHDJPIB());
		}
	}

	private IEnumerator GBBEKABFKEM()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private void LNIOFCGDDKP()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(AILKHBFIAJB());
		}
	}

	private void GFLAINNLMBO()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(AILKHBFIAJB());
		}
	}

	private void MEJPBKFMAIG()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(DCJLBJBHNDM());
		}
	}

	private IEnumerator IHMDPKFAIII()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private void INGOODALACO()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(AOLPHFKPEKI());
		}
	}

	private IEnumerator KMBGOLLFLKG()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private void MHGPANIBBCM()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(HCLAPCLBPKK());
		}
	}

	private void LLDBPKHDPKI()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(HCLAPCLBPKK());
		}
	}

	private IEnumerator AOLPHFKPEKI()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private IEnumerator HCLAPCLBPKK()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private IEnumerator POLFAMGIMLD()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private IEnumerator KGJDJKCHMNN()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private IEnumerator AILKHBFIAJB()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private IEnumerator DMJJDOBMMEK()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private IEnumerator BFGJBKIMOOO()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private void LIFBFDKFMMH()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(IDNPNKLOFBL());
		}
	}

	private void IBFHEDHJDLH()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(IDNPNKLOFBL());
		}
	}

	private IEnumerator BBMNDCFADJI()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private void IFBKMAEMKNN()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(BBMNDCFADJI());
		}
	}

	private IEnumerator CKAKDGBMEGP()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private IEnumerator OGGGNDODCBK()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private IEnumerator IDNPNKLOFBL()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private void HJDPEBAPBPN()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(NNIPHJLOCAL());
		}
	}

	private IEnumerator NNIPHJLOCAL()
	{
		yield return new WaitWhile(() => !SteamManager.isKeyVerified);
		yield return StartCoroutine(Singleton<ResourcesManager>.Instance.LoadSkinSprite("skin." + imageID, imageID));
		GetComponent<Image>().sprite = (Sprite)ResourcesManager.GetLoadedResource("skin." + imageID);
		ABLMLLLDOPO = true;
	}

	private void EJNKBOLGHJC()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(BFGJBKIMOOO());
		}
	}

	private void OCHMKJKBAPI()
	{
		if (!ABLMLLLDOPO)
		{
			StartCoroutine(CKAKDGBMEGP());
		}
	}
}
