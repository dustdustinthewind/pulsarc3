// NewsTile
using UnityEngine;
using UnityEngine.UI;

public class NewsTile : MonoBehaviour
{
	public Text nameText;

	public Text descriptionText;

	public Image image;

	public Image splashImage;

	public Slider newsIDSlider;

	private string IEAEOKEAJMA;

	public void OEGDGBIKNGG()
	{
		Singleton<GameManager>.Instance.CKMMHJLDADB(IEAEOKEAJMA);
	}

	public void NLCDDGGHAPI(float JMMILEFMACB)
	{
	}

	public void LPIKIPFHNMK(float JMMILEFMACB)
	{
	}

	public void NAGJBLFILID(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	private void KMEONPMCNJG()
	{
	}

	public void AEMEHKANNCA(float JMMILEFMACB)
	{
	}

	public void OPECOFMFIHF(float JMMILEFMACB)
	{
	}

	public void EIIFDMIHMGP(float JMMILEFMACB)
	{
	}

	private void EHJJFJCKGAJ()
	{
	}

	public void PCHKMGNOEDF()
	{
		Singleton<GameManager>.Instance.COLPGBJFLGM(IEAEOKEAJMA, false);
	}

	public void CFOANOEMLAA(float JMMILEFMACB)
	{
	}

	public void KDABDGBBAAN()
	{
		Singleton<GameManager>.Instance.OpenURL(IEAEOKEAJMA, false);
	}

	private void AGMJDGHLBMN()
	{
	}

	private void JNBPKNNBMDI()
	{
	}

	public void FENIFPLLDAK(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	public void CFNDDGFGKKG(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	public void CNNIGANMKJD()
	{
		Singleton<GameManager>.Instance.GMOJGDKBHEL(IEAEOKEAJMA, false);
	}

	public void HFGANPKDCJI()
	{
		Singleton<GameManager>.Instance.OLIINGPOEIE(IEAEOKEAJMA);
	}

	public void KFIMBEGHCIO(float JMMILEFMACB)
	{
	}

	public void AKMLEJJBBNG(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	public void OLDFANEHLMN(float JMMILEFMACB)
	{
	}

	private void BMODOIJGIOO()
	{
	}

	private void EJFJENFKLND()
	{
	}

	public void NOOKJIBJIJF()
	{
		Singleton<GameManager>.Instance.GMOJGDKBHEL(IEAEOKEAJMA, false);
	}

	public void HPKNKMOCCHJ()
	{
		Singleton<GameManager>.Instance.OpenURL(IEAEOKEAJMA, false);
	}

	public void FPHMHFFPLHM()
	{
		Singleton<GameManager>.Instance.CKMMHJLDADB(IEAEOKEAJMA);
	}

	public void MDCMMFEEMGH(float JMMILEFMACB)
	{
	}

	public void BGNEIGDAKOA(float JMMILEFMACB)
	{
	}

	public void Init(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	private void DDBOODLPCAM()
	{
	}

	private void Update()
	{
	}

	public void IIOMAHMEMED()
	{
		Singleton<GameManager>.Instance.GMOJGDKBHEL(IEAEOKEAJMA, false);
	}

	private void OPCLBGHAPMG()
	{
	}

	public void JEFKEOFNKIM(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	public void EGDJAFKIBMO(float JMMILEFMACB)
	{
	}

	public void EIPPDLDJJGO(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	private void DNENFLNCIJP()
	{
	}

	public void HABHCAAKDPL()
	{
		Singleton<GameManager>.Instance.CKMMHJLDADB(IEAEOKEAJMA);
	}

	public void CJFHNNICLEN()
	{
		Singleton<GameManager>.Instance.GMOJGDKBHEL(IEAEOKEAJMA, false);
	}

	public void GFKOENNHCEA(float JMMILEFMACB)
	{
	}

	private void AEEGKLABFLN()
	{
	}

	public void EOPEMLDJAAO(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	public void BKGHHKKOKPF(float JMMILEFMACB)
	{
	}

	public void NBHNONHLCAG()
	{
		Singleton<GameManager>.Instance.OLIINGPOEIE(IEAEOKEAJMA, false);
	}

	public void KMLHFIHIAON(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	public void POIOBHFFOLF(float JMMILEFMACB)
	{
	}

	private void NEKCPLGFOFD()
	{
	}

	public void KMEPPEEEGGP()
	{
		Singleton<GameManager>.Instance.COLPGBJFLGM(IEAEOKEAJMA, false);
	}

	public void LJCIGNEGKOD(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	public void BKEFJPOHKDE(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	public void ONFIKFDPHLL()
	{
		Singleton<GameManager>.Instance.GMOJGDKBHEL(IEAEOKEAJMA, false);
	}

	public void IFGOHEOEGKH(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	public void IMNOHPHOEMN()
	{
		Singleton<GameManager>.Instance.FMCBLGPDACK(IEAEOKEAJMA, false);
	}

	public void CDGPEDKIDDB()
	{
		Singleton<GameManager>.Instance.HEHKJIHIBLD(IEAEOKEAJMA, false);
	}

	public void HIOAOIEDMPK(float JMMILEFMACB)
	{
	}

	private void FOMNCPKKCFN()
	{
	}

	public void IHODAHDLCNJ(float JMMILEFMACB)
	{
	}

	public void KDJHONILJGK(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	public void KNJHPPILPKH()
	{
		Singleton<GameManager>.Instance.GMOJGDKBHEL(IEAEOKEAJMA, false);
	}

	private void HDDECCMEJKA()
	{
	}

	public void PIEPPKMBJHI(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	public void DABDPBIDDJH(float JMMILEFMACB)
	{
	}

	public void KEMDONMEPKH(float JMMILEFMACB)
	{
	}

	private void HIFLPHLGLFG()
	{
	}

	private void ONKDMMJPEMN()
	{
	}

	private void HLDFOJMHKNL()
	{
	}

	private void LPMINJJPDCH()
	{
	}

	public void KIBIGDNKOEM(float JMMILEFMACB)
	{
	}

	private void GHILDCBCDJI()
	{
	}

	private void JOACBIEHHCE()
	{
	}

	private void CGBHOELMAOC()
	{
	}

	public void PAGMFMLBFHL(float JMMILEFMACB)
	{
	}

	public void EFEONCCCKNP(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	public void CLPNGMEGNOC()
	{
		Singleton<GameManager>.Instance.CKMMHJLDADB(IEAEOKEAJMA);
	}

	public void LOEIABPCHLI(float JMMILEFMACB)
	{
	}

	private void ONMGIPAILOH()
	{
	}

	public void EKEEBBLDIGN(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	public void DDKHMCOOCPA(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	private void DPIPGGDNGHN()
	{
	}

	public void FECMEIJGLJI(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	public void NJNKMDAGPAN(float JMMILEFMACB)
	{
	}

	private void AAPKBNDHBLI()
	{
	}

	private void LBPCODPOJAH()
	{
	}

	public void LNOFEKBCKLL()
	{
		Singleton<GameManager>.Instance.OLIINGPOEIE(IEAEOKEAJMA, false);
	}

	public void FEJAAGGMBOE(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	private void PHJJHFBLICM()
	{
	}

	public void EHPGHFJIGCK(float JMMILEFMACB)
	{
	}

	public void HAKMJMAFNIL(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	public void ILHHAJMENLA(float JMMILEFMACB)
	{
	}

	public void PGKFCCIKFPK(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	private void EHCGBJDFMHG()
	{
	}

	public void BOOPLBOEBLC()
	{
		Singleton<GameManager>.Instance.FMCBLGPDACK(IEAEOKEAJMA);
	}

	private void KFACDBHDAOD()
	{
	}

	public void IFNDGOGKPIB(float JMMILEFMACB)
	{
	}

	public void OnClick()
	{
		Singleton<GameManager>.Instance.OpenURL(IEAEOKEAJMA);
	}

	public void BDEGMPGLODD()
	{
		Singleton<GameManager>.Instance.CKMMHJLDADB(IEAEOKEAJMA, false);
	}

	public void SwichNews(float JMMILEFMACB)
	{
	}

	public void IFINBHBFCBB(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	public void LIMLHBICMPL()
	{
		Singleton<GameManager>.Instance.COLPGBJFLGM(IEAEOKEAJMA, false);
	}

	public void AGBKFGJHOFF(float JMMILEFMACB)
	{
	}

	public void GMEKOCDHGNH(float JMMILEFMACB)
	{
	}

	public void FHKHEAPCPJK(float JMMILEFMACB)
	{
	}

	private void LLJLDLLNFBH()
	{
	}

	public void PKHHBNELOGG(float JMMILEFMACB)
	{
	}

	private void BGDONBMDPGM()
	{
	}

	private void IBFJAOINHMK()
	{
	}

	private void EGEGNHLODAA()
	{
	}

	public void MKDLIBGJMLL()
	{
		Singleton<GameManager>.Instance.COLPGBJFLGM(IEAEOKEAJMA);
	}

	public void BEPPFEAKIMG()
	{
		Singleton<GameManager>.Instance.HEHKJIHIBLD(IEAEOKEAJMA);
	}

	public void MOKHHEMAJBJ(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	private void BFGIMALGMAJ()
	{
	}

	public void ODDHKPJIJGI()
	{
		Singleton<GameManager>.Instance.CKMMHJLDADB(IEAEOKEAJMA);
	}

	public void BDDNELHPLNI()
	{
		Singleton<GameManager>.Instance.CKMMHJLDADB(IEAEOKEAJMA);
	}

	private void ODCJJFBJJNF()
	{
	}

	public void FFBDGOBNNCO(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	public void LHAENMGOBMM(string IOHIHHEBPDK, string ADLFGFFKELL, Sprite BBFDELDIDOG, string IEAEOKEAJMA)
	{
		nameText.text = IOHIHHEBPDK;
		descriptionText.text = ADLFGFFKELL;
		if ((bool)BBFDELDIDOG)
		{
			image.sprite = BBFDELDIDOG;
			image.GetComponent<AspectRatioFitter>().aspectRatio = (float)BBFDELDIDOG.texture.width / (float)BBFDELDIDOG.texture.height;
		}
		this.IEAEOKEAJMA = IEAEOKEAJMA;
	}

	public void GFBDFGACCLH()
	{
		Singleton<GameManager>.Instance.FMCBLGPDACK(IEAEOKEAJMA);
	}

	public void MDDGMDDHICG()
	{
		Singleton<GameManager>.Instance.FMCBLGPDACK(IEAEOKEAJMA);
	}

	public void NDGEHBGPALP(float JMMILEFMACB)
	{
	}

	private void IDIIELPAMCJ()
	{
	}

	public void AFIMCAIFJDC(float JMMILEFMACB)
	{
	}
}
