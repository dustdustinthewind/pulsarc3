// Sequences
using DG.Tweening;
using UnityEngine;

public class Sequences : MonoBehaviour
{
	public Transform target;

	private void KCCIEMBMOBA()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(1211f, 1329f, true));
		sequence.Join(target.DORotate(new Vector3(259f, 215f, 182f), 1569f, RotateMode.FastBeyond360));
		sequence.Append(target.DOScaleY(498f, 1761f));
		sequence.Insert(476f, target.DOMoveX(1417f, sequence.Duration()).SetRelative());
		sequence.SetLoops(2, LoopType.Restart);
	}

	private void JILOMOBDPIA()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(396f, 1882f));
		sequence.Join(target.DORotate(new Vector3(503f, 172f, 1731f), 1159f));
		sequence.Append(target.DOScaleY(671f, 1051f));
		sequence.Insert(34f, target.DOMoveX(1069f, sequence.Duration()).SetRelative());
		sequence.SetLoops(8, LoopType.Yoyo);
	}

	private void BGDPIHMAACO()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(799f, 757f));
		sequence.Join(target.DORotate(new Vector3(1741f, 163f, 1708f), 1705f, RotateMode.FastBeyond360));
		sequence.Append(target.DOScaleY(30f, 817f));
		sequence.Insert(639f, target.DOMoveX(1359f, sequence.Duration()).SetRelative());
		sequence.SetLoops(6, LoopType.Restart);
	}

	private void Start()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(2f, 1f));
		sequence.Join(target.DORotate(new Vector3(0f, 135f, 0f), 1f));
		sequence.Append(target.DOScaleY(0.2f, 1f));
		sequence.Insert(0f, target.DOMoveX(4f, sequence.Duration()).SetRelative());
		sequence.SetLoops(4, LoopType.Yoyo);
	}

	private void CIPKEPDELJB()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(1235f, 892f, true));
		sequence.Join(target.DORotate(new Vector3(126f, 181f, 1192f), 838f));
		sequence.Append(target.DOScaleY(513f, 1493f));
		sequence.Insert(1336f, target.DOMoveX(1783f, sequence.Duration(false)).SetRelative());
		sequence.SetLoops(8, LoopType.Restart);
	}

	private void FHPFJBFCOOF()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(179f, 1844f, true));
		sequence.Join(target.DORotate(new Vector3(264f, 1723f, 1380f), 1844f));
		sequence.Append(target.DOScaleY(912f, 1429f));
		sequence.Insert(1917f, target.DOMoveX(906f, sequence.Duration(), true).SetRelative());
		sequence.SetLoops(8, LoopType.Yoyo);
	}

	private void BMIOFJFMCBG()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(158f, 119f));
		sequence.Join(target.DORotate(new Vector3(1662f, 1248f, 1234f), 1128f));
		sequence.Append(target.DOScaleY(146f, 1451f));
		sequence.Insert(1428f, target.DOMoveX(1375f, sequence.Duration(false), true).SetRelative());
		sequence.SetLoops(2, LoopType.Restart);
	}

	private void KIMMMCJFMAB()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(1884f, 1137f));
		sequence.Join(target.DORotate(new Vector3(1650f, 1674f, 813f), 777f));
		sequence.Append(target.DOScaleY(169f, 1713f));
		sequence.Insert(1471f, target.DOMoveX(1535f, sequence.Duration(false)).SetRelative());
		sequence.SetLoops(7, LoopType.Yoyo);
	}

	private void HLIAEEMGBHN()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(1375f, 1877f));
		sequence.Join(target.DORotate(new Vector3(535f, 1698f, 1764f), 506f, RotateMode.FastBeyond360));
		sequence.Append(target.DOScaleY(1564f, 925f));
		sequence.Insert(710f, target.DOMoveX(739f, sequence.Duration(), true).SetRelative());
		sequence.SetLoops(1, LoopType.Yoyo);
	}

	private void HMPGIFBJFIK()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(1795f, 1170f, true));
		sequence.Join(target.DORotate(new Vector3(1070f, 1007f, 1144f), 164f, RotateMode.FastBeyond360));
		sequence.Append(target.DOScaleY(975f, 1086f));
		sequence.Insert(1190f, target.DOMoveX(1943f, sequence.Duration(), true).SetRelative());
		sequence.SetLoops(1, LoopType.Restart);
	}

	private void NCNPAKFAFOE()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(116f, 308f, true));
		sequence.Join(target.DORotate(new Vector3(1087f, 1927f, 504f), 962f));
		sequence.Append(target.DOScaleY(1442f, 355f));
		sequence.Insert(122f, target.DOMoveX(646f, sequence.Duration(false), true).SetRelative());
		sequence.SetLoops(6, LoopType.Restart);
	}

	private void KDMKDEKCELE()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(1658f, 1911f));
		sequence.Join(target.DORotate(new Vector3(1978f, 991f, 922f), 1263f, RotateMode.FastBeyond360));
		sequence.Append(target.DOScaleY(1690f, 1643f));
		sequence.Insert(794f, target.DOMoveX(268f, sequence.Duration(), true).SetRelative());
		sequence.SetLoops(8, LoopType.Yoyo);
	}

	private void EPEGAEGDJAM()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(886f, 525f));
		sequence.Join(target.DORotate(new Vector3(775f, 1428f, 537f), 568f));
		sequence.Append(target.DOScaleY(1885f, 1920f));
		sequence.Insert(1653f, target.DOMoveX(1155f, sequence.Duration(false), true).SetRelative());
		sequence.SetLoops(3, LoopType.Restart);
	}

	private void PAKPHKPDKGE()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(1151f, 609f));
		sequence.Join(target.DORotate(new Vector3(508f, 290f, 1277f), 745f, RotateMode.FastBeyond360));
		sequence.Append(target.DOScaleY(1209f, 49f));
		sequence.Insert(827f, target.DOMoveX(1942f, sequence.Duration()).SetRelative());
		sequence.SetLoops(3, LoopType.Restart);
	}

	private void NBGIMIDICKE()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(246f, 1478f));
		sequence.Join(target.DORotate(new Vector3(313f, 1627f, 832f), 124f));
		sequence.Append(target.DOScaleY(376f, 1115f));
		sequence.Insert(1143f, target.DOMoveX(618f, sequence.Duration()).SetRelative());
		sequence.SetLoops(8, LoopType.Restart);
	}

	private void CGDMLHLJIDK()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(1959f, 169f, true));
		sequence.Join(target.DORotate(new Vector3(906f, 1031f, 301f), 1579f));
		sequence.Append(target.DOScaleY(878f, 491f));
		sequence.Insert(516f, target.DOMoveX(83f, sequence.Duration(false), true).SetRelative());
		sequence.SetLoops(0, LoopType.Yoyo);
	}

	private void DAHFFNNIGML()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(588f, 489f, true));
		sequence.Join(target.DORotate(new Vector3(1679f, 50f, 225f), 498f, RotateMode.FastBeyond360));
		sequence.Append(target.DOScaleY(348f, 1506f));
		sequence.Insert(516f, target.DOMoveX(1588f, sequence.Duration(), true).SetRelative());
		sequence.SetLoops(1, LoopType.Restart);
	}

	private void KLILJHJNICK()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(1406f, 1265f));
		sequence.Join(target.DORotate(new Vector3(793f, 425f, 370f), 1529f));
		sequence.Append(target.DOScaleY(1911f, 1984f));
		sequence.Insert(1499f, target.DOMoveX(1747f, sequence.Duration()).SetRelative());
		sequence.SetLoops(8, LoopType.Yoyo);
	}

	private void DIPDEHOOBPG()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(1262f, 1159f));
		sequence.Join(target.DORotate(new Vector3(1843f, 612f, 1778f), 1724f));
		sequence.Append(target.DOScaleY(510f, 1536f));
		sequence.Insert(608f, target.DOMoveX(1548f, sequence.Duration(false)).SetRelative());
		sequence.SetLoops(1, LoopType.Yoyo);
	}

	private void DLBODOFAJGM()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(926f, 787f, true));
		sequence.Join(target.DORotate(new Vector3(389f, 1446f, 1069f), 1213f));
		sequence.Append(target.DOScaleY(1038f, 475f));
		sequence.Insert(574f, target.DOMoveX(328f, sequence.Duration(false)).SetRelative());
		sequence.SetLoops(4, LoopType.Restart);
	}

	private void DKOPKPBLDHH()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(461f, 373f, true));
		sequence.Join(target.DORotate(new Vector3(1025f, 831f, 51f), 28f));
		sequence.Append(target.DOScaleY(381f, 793f));
		sequence.Insert(1077f, target.DOMoveX(164f, sequence.Duration(false), true).SetRelative());
		sequence.SetLoops(5, LoopType.Yoyo);
	}

	private void AGEJKLMJGDO()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(873f, 1381f, true));
		sequence.Join(target.DORotate(new Vector3(1836f, 1168f, 296f), 1467f, RotateMode.FastBeyond360));
		sequence.Append(target.DOScaleY(1824f, 513f));
		sequence.Insert(140f, target.DOMoveX(61f, sequence.Duration()).SetRelative());
		sequence.SetLoops(0, LoopType.Restart);
	}

	private void KMCPMOGKDEH()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(548f, 1823f));
		sequence.Join(target.DORotate(new Vector3(642f, 639f, 1287f), 1147f, RotateMode.FastBeyond360));
		sequence.Append(target.DOScaleY(953f, 1476f));
		sequence.Insert(1493f, target.DOMoveX(1212f, sequence.Duration(false)).SetRelative());
		sequence.SetLoops(4, LoopType.Restart);
	}

	private void BEBNOKFLJPH()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(319f, 1770f));
		sequence.Join(target.DORotate(new Vector3(537f, 418f, 633f), 765f));
		sequence.Append(target.DOScaleY(1933f, 1263f));
		sequence.Insert(1577f, target.DOMoveX(890f, sequence.Duration(), true).SetRelative());
		sequence.SetLoops(2, LoopType.Yoyo);
	}

	private void IHLMNAGPKDA()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(1066f, 1952f));
		sequence.Join(target.DORotate(new Vector3(689f, 1045f, 993f), 1060f, RotateMode.FastBeyond360));
		sequence.Append(target.DOScaleY(47f, 673f));
		sequence.Insert(731f, target.DOMoveX(1467f, sequence.Duration()).SetRelative());
		sequence.SetLoops(0, LoopType.Yoyo);
	}

	private void JKFDDNMPOJH()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(222f, 840f));
		sequence.Join(target.DORotate(new Vector3(1142f, 1389f, 313f), 379f));
		sequence.Append(target.DOScaleY(635f, 1065f));
		sequence.Insert(57f, target.DOMoveX(802f, sequence.Duration(false)).SetRelative());
		sequence.SetLoops(6, LoopType.Yoyo);
	}

	private void KDMANOEMOCA()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(1443f, 55f, true));
		sequence.Join(target.DORotate(new Vector3(1521f, 1778f, 186f), 130f));
		sequence.Append(target.DOScaleY(672f, 1935f));
		sequence.Insert(733f, target.DOMoveX(344f, sequence.Duration(false), true).SetRelative());
		sequence.SetLoops(8, LoopType.Restart);
	}

	private void FEHCNJLLJMP()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(1669f, 965f, true));
		sequence.Join(target.DORotate(new Vector3(0f, 1773f, 1199f), 1707f));
		sequence.Append(target.DOScaleY(1138f, 1269f));
		sequence.Insert(1916f, target.DOMoveX(1887f, sequence.Duration()).SetRelative());
		sequence.SetLoops(7, LoopType.Restart);
	}

	private void IMCKJCHKMKB()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(186f, 1477f));
		sequence.Join(target.DORotate(new Vector3(879f, 369f, 1420f), 423f));
		sequence.Append(target.DOScaleY(1618f, 1256f));
		sequence.Insert(1256f, target.DOMoveX(622f, sequence.Duration(), true).SetRelative());
		sequence.SetLoops(5, LoopType.Restart);
	}

	private void CCLNNLCOPBL()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(344f, 1825f));
		sequence.Join(target.DORotate(new Vector3(650f, 95f, 783f), 1244f, RotateMode.FastBeyond360));
		sequence.Append(target.DOScaleY(703f, 631f));
		sequence.Insert(525f, target.DOMoveX(487f, sequence.Duration(), true).SetRelative());
		sequence.SetLoops(4, LoopType.Yoyo);
	}

	private void CNGAJDBOCLJ()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(1264f, 1025f));
		sequence.Join(target.DORotate(new Vector3(1789f, 1603f, 606f), 1902f, RotateMode.FastBeyond360));
		sequence.Append(target.DOScaleY(1147f, 15f));
		sequence.Insert(1342f, target.DOMoveX(113f, sequence.Duration()).SetRelative());
		sequence.SetLoops(8, LoopType.Restart);
	}

	private void DNNFHBOOPIN()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(1087f, 1300f));
		sequence.Join(target.DORotate(new Vector3(1796f, 160f, 1052f), 733f, RotateMode.FastBeyond360));
		sequence.Append(target.DOScaleY(304f, 200f));
		sequence.Insert(636f, target.DOMoveX(113f, sequence.Duration(false)).SetRelative());
		sequence.SetLoops(8, LoopType.Restart);
	}

	private void NNCCPEBIAKH()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(1892f, 653f));
		sequence.Join(target.DORotate(new Vector3(1121f, 1234f, 71f), 1373f, RotateMode.FastBeyond360));
		sequence.Append(target.DOScaleY(468f, 18f));
		sequence.Insert(281f, target.DOMoveX(1711f, sequence.Duration(false), true).SetRelative());
		sequence.SetLoops(4, LoopType.Restart);
	}

	private void MMOKKAPFGAK()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(1965f, 750f, true));
		sequence.Join(target.DORotate(new Vector3(651f, 1156f, 747f), 923f));
		sequence.Append(target.DOScaleY(1162f, 1186f));
		sequence.Insert(1900f, target.DOMoveX(1367f, sequence.Duration(), true).SetRelative());
		sequence.SetLoops(1, LoopType.Yoyo);
	}

	private void MMMDPANNAIO()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(16f, 1270f));
		sequence.Join(target.DORotate(new Vector3(1269f, 1736f, 814f), 119f, RotateMode.FastBeyond360));
		sequence.Append(target.DOScaleY(393f, 234f));
		sequence.Insert(1584f, target.DOMoveX(531f, sequence.Duration(false), true).SetRelative());
		sequence.SetLoops(0, LoopType.Restart);
	}

	private void FIKFJDFELIP()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(469f, 48f, true));
		sequence.Join(target.DORotate(new Vector3(386f, 336f, 293f), 1259f, RotateMode.FastBeyond360));
		sequence.Append(target.DOScaleY(1476f, 1685f));
		sequence.Insert(296f, target.DOMoveX(24f, sequence.Duration(false)).SetRelative());
		sequence.SetLoops(5, LoopType.Restart);
	}

	private void EGEPLFGKGLI()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(1387f, 1433f));
		sequence.Join(target.DORotate(new Vector3(431f, 1512f, 492f), 1875f, RotateMode.FastBeyond360));
		sequence.Append(target.DOScaleY(866f, 348f));
		sequence.Insert(1913f, target.DOMoveX(1106f, sequence.Duration(false)).SetRelative());
		sequence.SetLoops(1, LoopType.Restart);
	}

	private void NPLCENPNJBM()
	{
		Sequence sequence = DOTween.Sequence();
		sequence.Append(target.DOMoveY(1459f, 1142f, true));
		sequence.Join(target.DORotate(new Vector3(1402f, 1832f, 98f), 1913f));
		sequence.Append(target.DOScaleY(1292f, 1952f));
		sequence.Insert(757f, target.DOMoveX(1790f, sequence.Duration(false), true).SetRelative());
		sequence.SetLoops(4, LoopType.Yoyo);
	}
}
