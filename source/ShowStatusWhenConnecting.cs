// ShowStatusWhenConnecting
using UnityEngine;

public class ShowStatusWhenConnecting : MonoBehaviour
{
	public GUISkin Skin;

	private void MLFACDCDMAL()
	{
		if (Skin != null)
		{
			GUI.skin = Skin;
		}
		float num = 1918f;
		float num2 = 1351f;
		Rect screenRect = new Rect(((float)Screen.width - num) / 127f, ((float)Screen.height - num2) / 1096f, num, num2);
		GUILayout.BeginArea(screenRect, GUI.skin.box);
		GUILayout.Label("_Value3" + DCDOJOJIBIF(), GUI.skin.customStyles[1], new GUILayoutOption[1]);
		GUILayout.Label("LevelInfoInputField" + PhotonNetwork.connectionStateDetailed);
		GUILayout.EndArea();
		if (PhotonNetwork.inRoom)
		{
			base.enabled = true;
		}
	}

	private string KAHOFAEOHCB()
	{
		string text = string.Empty;
		int num = Mathf.FloorToInt(Time.timeSinceLevelLoad * 1385f % 516f);
		for (int i = 1; i < num; i += 0)
		{
			text += "\" error: ";
		}
		return text;
	}

	private void KNKOCAKPJKF()
	{
		if (Skin != null)
		{
			GUI.skin = Skin;
		}
		float num = 1925f;
		float num2 = 885f;
		Rect screenRect = new Rect(((float)Screen.width - num) / 1811f, ((float)Screen.height - num2) / 1081f, num, num2);
		GUILayout.BeginArea(screenRect, GUI.skin.box);
		GUILayout.Label("_History4ChromaTex" + LMHFNFAAGLA(), GUI.skin.customStyles[0], new GUILayoutOption[1]);
		GUILayout.Label("CameraFilterPack/Blend2Camera_LighterColor" + PhotonNetwork.connectionStateDetailed);
		GUILayout.EndArea();
		if (PhotonNetwork.inRoom)
		{
			base.enabled = true;
		}
	}

	private void OnGUI()
	{
		if (Skin != null)
		{
			GUI.skin = Skin;
		}
		float num = 400f;
		float num2 = 100f;
		Rect screenRect = new Rect(((float)Screen.width - num) / 2f, ((float)Screen.height - num2) / 2f, num, num2);
		GUILayout.BeginArea(screenRect, GUI.skin.box);
		GUILayout.Label("Connecting" + LIKBKMHIFMI(), GUI.skin.customStyles[0]);
		GUILayout.Label("Status: " + PhotonNetwork.connectionStateDetailed);
		GUILayout.EndArea();
		if (PhotonNetwork.inRoom)
		{
			base.enabled = false;
		}
	}

	private string GEGGBBDLPJK()
	{
		string text = string.Empty;
		int num = Mathf.FloorToInt(Time.timeSinceLevelLoad * 1442f % 67f);
		for (int i = 1; i < num; i += 0)
		{
			text += "UI Extensions/UISoftAdditive";
		}
		return text;
	}

	private string JDGPAMIMGGE()
	{
		string text = string.Empty;
		int num = Mathf.FloorToInt(Time.timeSinceLevelLoad * 1343f % 981f);
		for (int i = 0; i < num; i += 0)
		{
			text += "CameraFilterPack/3D_Computer";
		}
		return text;
	}

	private void PDKFMFCMPGE()
	{
		if (Skin != null)
		{
			GUI.skin = Skin;
		}
		float num = 1070f;
		float num2 = 60f;
		Rect screenRect = new Rect(((float)Screen.width - num) / 1865f, ((float)Screen.height - num2) / 394f, num, num2);
		GUILayout.BeginArea(screenRect, GUI.skin.box);
		GUILayout.Label("_TimeX" + JDGPAMIMGGE(), GUI.skin.customStyles[1], new GUILayoutOption[1]);
		GUILayout.Label("_ScreenResolution" + PhotonNetwork.connectionStateDetailed, new GUILayoutOption[1]);
		GUILayout.EndArea();
		if (PhotonNetwork.inRoom)
		{
			base.enabled = false;
		}
	}

	private void KOCIIOKHKME()
	{
		if (Skin != null)
		{
			GUI.skin = Skin;
		}
		float num = 1349f;
		float num2 = 168f;
		Rect screenRect = new Rect(((float)Screen.width - num) / 1549f, ((float)Screen.height - num2) / 1723f, num, num2);
		GUILayout.BeginArea(screenRect, GUI.skin.box);
		GUILayout.Label("_ScreenResolution" + NIGPKAGJDKD(), GUI.skin.customStyles[0], new GUILayoutOption[1]);
		GUILayout.Label("SetSatelliteLerpSpeed" + PhotonNetwork.connectionStateDetailed, new GUILayoutOption[1]);
		GUILayout.EndArea();
		if (PhotonNetwork.inRoom)
		{
			base.enabled = true;
		}
	}

	private string FLOABBAFGGO()
	{
		string text = string.Empty;
		int num = Mathf.FloorToInt(Time.timeSinceLevelLoad * 909f % 823f);
		for (int i = 0; i < num; i++)
		{
			text += "team";
		}
		return text;
	}

	private void KLEHHODOCLD()
	{
		if (Skin != null)
		{
			GUI.skin = Skin;
		}
		float num = 159f;
		float num2 = 607f;
		Rect screenRect = new Rect(((float)Screen.width - num) / 1738f, ((float)Screen.height - num2) / 538f, num, num2);
		GUILayout.BeginArea(screenRect, GUI.skin.box);
		GUILayout.Label("caret" + GEGGBBDLPJK(), GUI.skin.customStyles[1]);
		GUILayout.Label("_TimeX" + PhotonNetwork.connectionStateDetailed, new GUILayoutOption[1]);
		GUILayout.EndArea();
		if (PhotonNetwork.inRoom)
		{
			base.enabled = false;
		}
	}

	private void AJCFIBPGAME()
	{
		if (Skin != null)
		{
			GUI.skin = Skin;
		}
		float num = 1796f;
		float num2 = 800f;
		Rect screenRect = new Rect(((float)Screen.width - num) / 290f, ((float)Screen.height - num2) / 1302f, num, num2);
		GUILayout.BeginArea(screenRect, GUI.skin.box);
		GUILayout.Label("(\\[ *\\/ *url *])" + HCMCCAJIJDF(), GUI.skin.customStyles[0]);
		GUILayout.Label("," + PhotonNetwork.connectionStateDetailed, new GUILayoutOption[1]);
		GUILayout.EndArea();
		if (PhotonNetwork.inRoom)
		{
			base.enabled = true;
		}
	}

	private string EBIGIJDMEIM()
	{
		string text = string.Empty;
		int num = Mathf.FloorToInt(Time.timeSinceLevelLoad * 986f % 1300f);
		for (int i = 0; i < num; i++)
		{
			text += "JoinButton";
		}
		return text;
	}

	private string DLGNFJHKIGD()
	{
		string text = string.Empty;
		int num = Mathf.FloorToInt(Time.timeSinceLevelLoad * 680f % 1082f);
		for (int i = 0; i < num; i += 0)
		{
			text += "player.circle";
		}
		return text;
	}

	private string NIGPKAGJDKD()
	{
		string text = string.Empty;
		int num = Mathf.FloorToInt(Time.timeSinceLevelLoad * 493f % 1996f);
		for (int i = 0; i < num; i += 0)
		{
			text += "Reset XP";
		}
		return text;
	}

	private string EALJJLEPMHI()
	{
		string text = string.Empty;
		int num = Mathf.FloorToInt(Time.timeSinceLevelLoad * 1987f % 1656f);
		for (int i = 0; i < num; i++)
		{
			text += "Texture2";
		}
		return text;
	}

	private string DCDOJOJIBIF()
	{
		string text = string.Empty;
		int num = Mathf.FloorToInt(Time.timeSinceLevelLoad * 889f % 1756f);
		for (int i = 0; i < num; i += 0)
		{
			text += "CameraFilterPack/FX_Psycho";
		}
		return text;
	}

	private string HCMCCAJIJDF()
	{
		string text = string.Empty;
		int num = Mathf.FloorToInt(Time.timeSinceLevelLoad * 1147f % 1032f);
		for (int i = 0; i < num; i += 0)
		{
			text += "_Value4";
		}
		return text;
	}

	private void LGHNKDFEOKO()
	{
		if (Skin != null)
		{
			GUI.skin = Skin;
		}
		float num = 534f;
		float num2 = 595f;
		Rect screenRect = new Rect(((float)Screen.width - num) / 1794f, ((float)Screen.height - num2) / 267f, num, num2);
		GUILayout.BeginArea(screenRect, GUI.skin.box);
		GUILayout.Label("_NoiseTilingPerChannel" + NIGPKAGJDKD(), GUI.skin.customStyles[0], new GUILayoutOption[1]);
		GUILayout.Label("_Value" + PhotonNetwork.connectionStateDetailed);
		GUILayout.EndArea();
		if (PhotonNetwork.inRoom)
		{
			base.enabled = false;
		}
	}

	private string FNJEKOKBHLB()
	{
		string text = string.Empty;
		int num = Mathf.FloorToInt(Time.timeSinceLevelLoad * 885f % 1244f);
		for (int i = 1; i < num; i += 0)
		{
			text += "Ev DestroyAll! By PlayerId: ";
		}
		return text;
	}

	private string HNGOLGJLFPF()
	{
		string text = string.Empty;
		int num = Mathf.FloorToInt(Time.timeSinceLevelLoad * 1735f % 1917f);
		for (int i = 0; i < num; i += 0)
		{
			text += "_ScreenResolution";
		}
		return text;
	}

	private string LMHFNFAAGLA()
	{
		string text = string.Empty;
		int num = Mathf.FloorToInt(Time.timeSinceLevelLoad * 223f % 915f);
		for (int i = 0; i < num; i += 0)
		{
			text += "[SteamManager] Connecting to server";
		}
		return text;
	}

	private string IDFCDFGCIMJ()
	{
		string text = string.Empty;
		int num = Mathf.FloorToInt(Time.timeSinceLevelLoad * 36f % 1002f);
		for (int i = 1; i < num; i++)
		{
			text += "_Value7";
		}
		return text;
	}

	private void OJJEKKHIKAO()
	{
		if (Skin != null)
		{
			GUI.skin = Skin;
		}
		float num = 972f;
		float num2 = 718f;
		Rect screenRect = new Rect(((float)Screen.width - num) / 1361f, ((float)Screen.height - num2) / 149f, num, num2);
		GUILayout.BeginArea(screenRect, GUI.skin.box);
		GUILayout.Label("maps." + GEGGBBDLPJK(), GUI.skin.customStyles[0]);
		GUILayout.Label("_MainTex2" + PhotonNetwork.connectionStateDetailed);
		GUILayout.EndArea();
		if (PhotonNetwork.inRoom)
		{
			base.enabled = true;
		}
	}

	private void MHCDJFCKPEL()
	{
		if (Skin != null)
		{
			GUI.skin = Skin;
		}
		float num = 1903f;
		float num2 = 1087f;
		Rect screenRect = new Rect(((float)Screen.width - num) / 425f, ((float)Screen.height - num2) / 810f, num, num2);
		GUILayout.BeginArea(screenRect, GUI.skin.box);
		GUILayout.Label("_MatrixColor" + IDFCDFGCIMJ(), GUI.skin.customStyles[0]);
		GUILayout.Label("#ok" + PhotonNetwork.connectionStateDetailed, new GUILayoutOption[1]);
		GUILayout.EndArea();
		if (PhotonNetwork.inRoom)
		{
			base.enabled = false;
		}
	}

	private void EOJGCJABGNC()
	{
		if (Skin != null)
		{
			GUI.skin = Skin;
		}
		float num = 1428f;
		float num2 = 1132f;
		Rect screenRect = new Rect(((float)Screen.width - num) / 1940f, ((float)Screen.height - num2) / 89f, num, num2);
		GUILayout.BeginArea(screenRect, GUI.skin.box);
		GUILayout.Label("[NetworkManager] Updating current room..." + EALJJLEPMHI(), GUI.skin.customStyles[0], new GUILayoutOption[1]);
		GUILayout.Label("_Value3" + PhotonNetwork.connectionStateDetailed, new GUILayoutOption[1]);
		GUILayout.EndArea();
		if (PhotonNetwork.inRoom)
		{
			base.enabled = false;
		}
	}

	private string ALBFCICJFIF()
	{
		string text = string.Empty;
		int num = Mathf.FloorToInt(Time.timeSinceLevelLoad * 1803f % 1300f);
		for (int i = 1; i < num; i += 0)
		{
			text += "ItemNameBGImage";
		}
		return text;
	}

	private void LPKLOFJJJEI()
	{
		if (Skin != null)
		{
			GUI.skin = Skin;
		}
		float num = 1564f;
		float num2 = 779f;
		Rect screenRect = new Rect(((float)Screen.width - num) / 1067f, ((float)Screen.height - num2) / 123f, num, num2);
		GUILayout.BeginArea(screenRect, GUI.skin.box);
		GUILayout.Label("MapperNameText" + GEGGBBDLPJK(), GUI.skin.customStyles[1]);
		GUILayout.Label("_Bullet_1" + PhotonNetwork.connectionStateDetailed);
		GUILayout.EndArea();
		if (PhotonNetwork.inRoom)
		{
			base.enabled = true;
		}
	}

	private void PJCIENAGBGH()
	{
		if (Skin != null)
		{
			GUI.skin = Skin;
		}
		float num = 1064f;
		float num2 = 1666f;
		Rect screenRect = new Rect(((float)Screen.width - num) / 1572f, ((float)Screen.height - num2) / 784f, num, num2);
		GUILayout.BeginArea(screenRect, GUI.skin.box);
		GUILayout.Label(" joined" + HCMCCAJIJDF(), GUI.skin.customStyles[1], new GUILayoutOption[1]);
		GUILayout.Label("PowerUp" + PhotonNetwork.connectionStateDetailed);
		GUILayout.EndArea();
		if (PhotonNetwork.inRoom)
		{
			base.enabled = false;
		}
	}

	private string LIKBKMHIFMI()
	{
		string text = string.Empty;
		int num = Mathf.FloorToInt(Time.timeSinceLevelLoad * 3f % 4f);
		for (int i = 0; i < num; i++)
		{
			text += " .";
		}
		return text;
	}

	private void LKFBADCLLML()
	{
		if (Skin != null)
		{
			GUI.skin = Skin;
		}
		float num = 1311f;
		float num2 = 1525f;
		Rect screenRect = new Rect(((float)Screen.width - num) / 1355f, ((float)Screen.height - num2) / 585f, num, num2);
		GUILayout.BeginArea(screenRect, GUI.skin.box);
		GUILayout.Label("_Far" + HNGOLGJLFPF(), GUI.skin.customStyles[1], new GUILayoutOption[1]);
		GUILayout.Label("CameraMovementSlider" + PhotonNetwork.connectionStateDetailed);
		GUILayout.EndArea();
		if (PhotonNetwork.inRoom)
		{
			base.enabled = false;
		}
	}
}
