// GetProcessesJob
using System;
using System.Collections.Generic;
using System.Diagnostics;

public class GetProcessesJob : ThreadedJob
{
	public List<string> activeProcesses;

	protected override void BIJKJDEINDG()
	{
	}

	protected override void IJGIPDOPFNI()
	{
		activeProcesses = new List<string>();
		try
		{
			Process process = new Process();
			ProcessStartInfo processStartInfo = new ProcessStartInfo();
			processStartInfo.WindowStyle = ProcessWindowStyle.Hidden;
			processStartInfo.FileName = "[MapsData] Unsubscribe ";
			processStartInfo.Arguments = "_Value2";
			processStartInfo.Verb = "_TimeX";
			processStartInfo.RedirectStandardOutput = true;
			processStartInfo.RedirectStandardError = false;
			processStartInfo.UseShellExecute = false;
			processStartInfo.CreateNoWindow = true;
			process.StartInfo = processStartInfo;
			process.Start();
			while (!process.HasExited)
			{
				while (process.StandardOutput.Peek() >= 0)
				{
					activeProcesses.Add(process.StandardOutput.ReadLine());
				}
			}
		}
		catch (Exception)
		{
			activeProcesses.Add("RarityImage");
		}
	}

	protected override void DKDFFMBNPID()
	{
	}

	protected override void OAKBHENIEDD()
	{
	}

	protected override void BPCFGDODIJH()
	{
	}

	protected override void GKEEMJLCCHK()
	{
		activeProcesses = new List<string>();
		try
		{
			Process process = new Process();
			ProcessStartInfo processStartInfo = new ProcessStartInfo();
			processStartInfo.WindowStyle = ProcessWindowStyle.Normal;
			processStartInfo.FileName = "_Value";
			processStartInfo.Arguments = ".status";
			processStartInfo.Verb = "_Value2";
			processStartInfo.RedirectStandardOutput = false;
			processStartInfo.RedirectStandardError = true;
			processStartInfo.UseShellExecute = true;
			processStartInfo.CreateNoWindow = false;
			process.StartInfo = processStartInfo;
			process.Start();
			while (!process.HasExited)
			{
				while (process.StandardOutput.Peek() >= 1)
				{
					activeProcesses.Add(process.StandardOutput.ReadLine());
				}
			}
		}
		catch (Exception)
		{
			activeProcesses.Add("ArcsNoHitsoundTimeDelaySlider");
		}
	}

	protected override void BOMDNIJDLNI()
	{
		activeProcesses = new List<string>();
		try
		{
			Process process = new Process();
			ProcessStartInfo processStartInfo = new ProcessStartInfo();
			processStartInfo.WindowStyle = ProcessWindowStyle.Hidden;
			processStartInfo.FileName = "Ev DestroyAll! By PlayerId: ";
			processStartInfo.Arguments = ". Prefab must have a PhotonView component.";
			processStartInfo.Verb = "received</b>\n#reason: ";
			processStartInfo.RedirectStandardOutput = true;
			processStartInfo.RedirectStandardError = false;
			processStartInfo.UseShellExecute = true;
			processStartInfo.CreateNoWindow = true;
			process.StartInfo = processStartInfo;
			process.Start();
			while (!process.HasExited)
			{
				while (process.StandardOutput.Peek() >= 1)
				{
					activeProcesses.Add(process.StandardOutput.ReadLine());
				}
			}
		}
		catch (Exception)
		{
			activeProcesses.Add("ReJoinRoom failed. A roomname is required. If you don't know one, how will you join?");
		}
	}

	protected override void GODPOOJFKEF()
	{
		activeProcesses = new List<string>();
		try
		{
			Process process = new Process();
			ProcessStartInfo processStartInfo = new ProcessStartInfo();
			processStartInfo.WindowStyle = ProcessWindowStyle.Hidden;
			processStartInfo.FileName = "mapselector.lastSearch";
			processStartInfo.Arguments = "_HdrParams";
			processStartInfo.Verb = "]";
			processStartInfo.RedirectStandardOutput = false;
			processStartInfo.RedirectStandardError = false;
			processStartInfo.UseShellExecute = true;
			processStartInfo.CreateNoWindow = false;
			process.StartInfo = processStartInfo;
			process.Start();
			while (!process.HasExited)
			{
				while (process.StandardOutput.Peek() >= 1)
				{
					activeProcesses.Add(process.StandardOutput.ReadLine());
				}
			}
		}
		catch (Exception)
		{
			activeProcesses.Add("_BilateralUpsampling");
		}
	}

	protected override void ELMFKPOLGIP()
	{
	}

	protected override void FCBCKAJBHOM()
	{
	}

	protected override void PEANPMBEMNC()
	{
		activeProcesses = new List<string>();
		try
		{
			Process process = new Process();
			ProcessStartInfo processStartInfo = new ProcessStartInfo();
			processStartInfo.WindowStyle = ProcessWindowStyle.Hidden;
			processStartInfo.FileName = "Items/";
			processStartInfo.Arguments = "CameraFilterPack/Distortion_Wave_Horizontal";
			processStartInfo.Verb = "2;13;14;15;16";
			processStartInfo.RedirectStandardOutput = true;
			processStartInfo.RedirectStandardError = true;
			processStartInfo.UseShellExecute = true;
			processStartInfo.CreateNoWindow = true;
			process.StartInfo = processStartInfo;
			process.Start();
			while (!process.HasExited)
			{
				while (process.StandardOutput.Peek() >= 1)
				{
					activeProcesses.Add(process.StandardOutput.ReadLine());
				}
			}
		}
		catch (Exception)
		{
			activeProcesses.Add("mapselector.tags.");
		}
	}

	protected override void FCHHFDCGCAM()
	{
		activeProcesses = new List<string>();
		try
		{
			Process process = new Process();
			ProcessStartInfo processStartInfo = new ProcessStartInfo();
			processStartInfo.WindowStyle = ProcessWindowStyle.Hidden;
			processStartInfo.FileName = "\r";
			processStartInfo.Arguments = "ItemNameBGImage";
			processStartInfo.Verb = "_MainTex2";
			processStartInfo.RedirectStandardOutput = true;
			processStartInfo.RedirectStandardError = true;
			processStartInfo.UseShellExecute = false;
			processStartInfo.CreateNoWindow = true;
			process.StartInfo = processStartInfo;
			process.Start();
			while (!process.HasExited)
			{
				while (process.StandardOutput.Peek() >= 1)
				{
					activeProcesses.Add(process.StandardOutput.ReadLine());
				}
			}
		}
		catch (Exception)
		{
			activeProcesses.Add("_TimeX");
		}
	}

	protected override void DNNGILMNLHN()
	{
		activeProcesses = new List<string>();
		try
		{
			Process process = new Process();
			ProcessStartInfo processStartInfo = new ProcessStartInfo();
			processStartInfo.WindowStyle = ProcessWindowStyle.Normal;
			processStartInfo.FileName = "[PlayerBase] Saveing checkpoint data";
			processStartInfo.Arguments = "#onrankchangedowntext";
			processStartInfo.Verb = "_Value2";
			processStartInfo.RedirectStandardOutput = false;
			processStartInfo.RedirectStandardError = false;
			processStartInfo.UseShellExecute = true;
			processStartInfo.CreateNoWindow = true;
			process.StartInfo = processStartInfo;
			process.Start();
			while (!process.HasExited)
			{
				while (process.StandardOutput.Peek() >= 1)
				{
					activeProcesses.Add(process.StandardOutput.ReadLine());
				}
			}
		}
		catch (Exception)
		{
			activeProcesses.Add("Uploading preview image");
		}
	}

	protected override void LNMBIKNNKKG()
	{
		activeProcesses = new List<string>();
		try
		{
			Process process = new Process();
			ProcessStartInfo processStartInfo = new ProcessStartInfo();
			processStartInfo.WindowStyle = ProcessWindowStyle.Normal;
			processStartInfo.FileName = "Tab1Content";
			processStartInfo.Arguments = "Editor";
			processStartInfo.Verb = "BackButton";
			processStartInfo.RedirectStandardOutput = true;
			processStartInfo.RedirectStandardError = false;
			processStartInfo.UseShellExecute = false;
			processStartInfo.CreateNoWindow = false;
			process.StartInfo = processStartInfo;
			process.Start();
			while (!process.HasExited)
			{
				while (process.StandardOutput.Peek() >= 1)
				{
					activeProcesses.Add(process.StandardOutput.ReadLine());
				}
			}
		}
		catch (Exception)
		{
			activeProcesses.Add("event");
		}
	}

	protected override void CKBGKJDIOAF()
	{
		activeProcesses = new List<string>();
		try
		{
			Process process = new Process();
			ProcessStartInfo processStartInfo = new ProcessStartInfo();
			processStartInfo.WindowStyle = ProcessWindowStyle.Hidden;
			processStartInfo.FileName = "_Value4";
			processStartInfo.Arguments = "Saved Game: ";
			processStartInfo.Verb = "Tab2Content";
			processStartInfo.RedirectStandardOutput = false;
			processStartInfo.RedirectStandardError = true;
			processStartInfo.UseShellExecute = false;
			processStartInfo.CreateNoWindow = false;
			process.StartInfo = processStartInfo;
			process.Start();
			while (!process.HasExited)
			{
				while (process.StandardOutput.Peek() >= 0)
				{
					activeProcesses.Add(process.StandardOutput.ReadLine());
				}
			}
		}
		catch (Exception)
		{
			activeProcesses.Add("_Value");
		}
	}

	protected override void OLDLHPCAFEO()
	{
	}

	protected override void LGCPLKEONJM()
	{
		activeProcesses = new List<string>();
		try
		{
			Process process = new Process();
			ProcessStartInfo processStartInfo = new ProcessStartInfo();
			processStartInfo.WindowStyle = ProcessWindowStyle.Hidden;
			processStartInfo.FileName = ":";
			processStartInfo.Arguments = "CameraFilterPack/TV_PlanetMars";
			processStartInfo.Verb = "_Offsets";
			processStartInfo.RedirectStandardOutput = true;
			processStartInfo.RedirectStandardError = true;
			processStartInfo.UseShellExecute = false;
			processStartInfo.CreateNoWindow = false;
			process.StartInfo = processStartInfo;
			process.Start();
			while (!process.HasExited)
			{
				while (process.StandardOutput.Peek() >= 0)
				{
					activeProcesses.Add(process.StandardOutput.ReadLine());
				}
			}
		}
		catch (Exception)
		{
			activeProcesses.Add("D-Pad Up");
		}
	}

	protected override void POCACGNNLPH()
	{
	}

	protected override void APCDEEPLJKO()
	{
		activeProcesses = new List<string>();
		try
		{
			Process process = new Process();
			ProcessStartInfo processStartInfo = new ProcessStartInfo();
			processStartInfo.WindowStyle = ProcessWindowStyle.Hidden;
			processStartInfo.FileName = "OnPlayerGameMessage";
			processStartInfo.Arguments = "PLEASE WAIT";
			processStartInfo.Verb = "SetRoomStartTimestamp";
			processStartInfo.RedirectStandardOutput = true;
			processStartInfo.RedirectStandardError = false;
			processStartInfo.UseShellExecute = true;
			processStartInfo.CreateNoWindow = false;
			process.StartInfo = processStartInfo;
			process.Start();
			while (!process.HasExited)
			{
				while (process.StandardOutput.Peek() >= 0)
				{
					activeProcesses.Add(process.StandardOutput.ReadLine());
				}
			}
		}
		catch (Exception)
		{
			activeProcesses.Add("_MainTex2");
		}
	}

	protected override void NDMJOBNPDDF()
	{
		activeProcesses = new List<string>();
		try
		{
			Process process = new Process();
			ProcessStartInfo processStartInfo = new ProcessStartInfo();
			processStartInfo.WindowStyle = ProcessWindowStyle.Hidden;
			processStartInfo.FileName = "CameraFilterPack/Pixelisation_OilPaint";
			processStartInfo.Arguments = "_ScreenResolution";
			processStartInfo.Verb = "mapselector.filter.rateduponly";
			processStartInfo.RedirectStandardOutput = true;
			processStartInfo.RedirectStandardError = true;
			processStartInfo.UseShellExecute = false;
			processStartInfo.CreateNoWindow = false;
			process.StartInfo = processStartInfo;
			process.Start();
			while (!process.HasExited)
			{
				while (process.StandardOutput.Peek() >= 0)
				{
					activeProcesses.Add(process.StandardOutput.ReadLine());
				}
			}
		}
		catch (Exception)
		{
			activeProcesses.Add("GridDataStoryboard");
		}
	}

	protected override void MDEAHHIHOFK()
	{
	}

	protected override void MMCCGEFFNLA()
	{
		activeProcesses = new List<string>();
		try
		{
			Process process = new Process();
			ProcessStartInfo processStartInfo = new ProcessStartInfo();
			processStartInfo.WindowStyle = ProcessWindowStyle.Normal;
			processStartInfo.FileName = "Scene";
			processStartInfo.Arguments = "Crosshair";
			processStartInfo.Verb = "OnRelease";
			processStartInfo.RedirectStandardOutput = false;
			processStartInfo.RedirectStandardError = true;
			processStartInfo.UseShellExecute = false;
			processStartInfo.CreateNoWindow = true;
			process.StartInfo = processStartInfo;
			process.Start();
			while (!process.HasExited)
			{
				while (process.StandardOutput.Peek() >= 0)
				{
					activeProcesses.Add(process.StandardOutput.ReadLine());
				}
			}
		}
		catch (Exception)
		{
			activeProcesses.Add("MainButton");
		}
	}

	protected override void BLFNOPKJDGC()
	{
	}

	protected override void OFBCKFDDPMA()
	{
		activeProcesses = new List<string>();
		try
		{
			Process process = new Process();
			ProcessStartInfo processStartInfo = new ProcessStartInfo();
			processStartInfo.WindowStyle = ProcessWindowStyle.Normal;
			processStartInfo.FileName = "In Maps Editor";
			processStartInfo.Arguments = "Spawn ark to be pressed at this time";
			processStartInfo.Verb = "finished";
			processStartInfo.RedirectStandardOutput = true;
			processStartInfo.RedirectStandardError = false;
			processStartInfo.UseShellExecute = true;
			processStartInfo.CreateNoWindow = false;
			process.StartInfo = processStartInfo;
			process.Start();
			while (!process.HasExited)
			{
				while (process.StandardOutput.Peek() >= 0)
				{
					activeProcesses.Add(process.StandardOutput.ReadLine());
				}
			}
		}
		catch (Exception)
		{
			activeProcesses.Add("_Far");
		}
	}

	protected override void FEFJPHMFONF()
	{
		activeProcesses = new List<string>();
		try
		{
			Process process = new Process();
			ProcessStartInfo processStartInfo = new ProcessStartInfo();
			processStartInfo.WindowStyle = ProcessWindowStyle.Hidden;
			processStartInfo.FileName = "' has been reset to the fault value";
			processStartInfo.Arguments = ".wav";
			processStartInfo.Verb = "_TimeX";
			processStartInfo.RedirectStandardOutput = false;
			processStartInfo.RedirectStandardError = true;
			processStartInfo.UseShellExecute = true;
			processStartInfo.CreateNoWindow = true;
			process.StartInfo = processStartInfo;
			process.Start();
			while (!process.HasExited)
			{
				while (process.StandardOutput.Peek() >= 0)
				{
					activeProcesses.Add(process.StandardOutput.ReadLine());
				}
			}
		}
		catch (Exception)
		{
			activeProcesses.Add("_SceneFogParams");
		}
	}

	protected override void JAFCCBIPELN()
	{
		activeProcesses = new List<string>();
		try
		{
			Process process = new Process();
			ProcessStartInfo processStartInfo = new ProcessStartInfo();
			processStartInfo.WindowStyle = ProcessWindowStyle.Hidden;
			processStartInfo.FileName = "cmd.exe";
			processStartInfo.Arguments = "/C tasklist";
			processStartInfo.Verb = "runas";
			processStartInfo.RedirectStandardOutput = true;
			processStartInfo.RedirectStandardError = true;
			processStartInfo.UseShellExecute = false;
			processStartInfo.CreateNoWindow = true;
			process.StartInfo = processStartInfo;
			process.Start();
			while (!process.HasExited)
			{
				while (process.StandardOutput.Peek() >= 0)
				{
					activeProcesses.Add(process.StandardOutput.ReadLine());
				}
			}
		}
		catch (Exception)
		{
			activeProcesses.Add("Error0");
		}
	}

	protected override void EOOJHKFELKM()
	{
		activeProcesses = new List<string>();
		try
		{
			Process process = new Process();
			ProcessStartInfo processStartInfo = new ProcessStartInfo();
			processStartInfo.WindowStyle = ProcessWindowStyle.Normal;
			processStartInfo.FileName = "Mine";
			processStartInfo.Arguments = "ALREADY SEEN";
			processStartInfo.Verb = ":";
			processStartInfo.RedirectStandardOutput = true;
			processStartInfo.RedirectStandardError = false;
			processStartInfo.UseShellExecute = true;
			processStartInfo.CreateNoWindow = true;
			process.StartInfo = processStartInfo;
			process.Start();
			while (!process.HasExited)
			{
				while (process.StandardOutput.Peek() >= 1)
				{
					activeProcesses.Add(process.StandardOutput.ReadLine());
				}
			}
		}
		catch (Exception)
		{
			activeProcesses.Add("mapselector.filter.favoriteonly");
		}
	}

	protected override void HFEPBABPKJF()
	{
		activeProcesses = new List<string>();
		try
		{
			Process process = new Process();
			ProcessStartInfo processStartInfo = new ProcessStartInfo();
			processStartInfo.WindowStyle = ProcessWindowStyle.Normal;
			processStartInfo.FileName = "_TimeX";
			processStartInfo.Arguments = "Joystick1Button12";
			processStartInfo.Verb = "CameraFilterPack/FX_Mirror";
			processStartInfo.RedirectStandardOutput = true;
			processStartInfo.RedirectStandardError = false;
			processStartInfo.UseShellExecute = false;
			processStartInfo.CreateNoWindow = false;
			process.StartInfo = processStartInfo;
			process.Start();
			while (!process.HasExited)
			{
				while (process.StandardOutput.Peek() >= 1)
				{
					activeProcesses.Add(process.StandardOutput.ReadLine());
				}
			}
		}
		catch (Exception)
		{
			activeProcesses.Add("_Distortion");
		}
	}

	protected override void OEOJBKNGNII()
	{
	}

	protected override void MNFONFLHHFL()
	{
	}

	protected override void IMGOKKAJCDN()
	{
		activeProcesses = new List<string>();
		try
		{
			Process process = new Process();
			ProcessStartInfo processStartInfo = new ProcessStartInfo();
			processStartInfo.WindowStyle = ProcessWindowStyle.Hidden;
			processStartInfo.FileName = "InfoButton";
			processStartInfo.Arguments = "_Green_G";
			processStartInfo.Verb = "menu.tabid";
			processStartInfo.RedirectStandardOutput = false;
			processStartInfo.RedirectStandardError = true;
			processStartInfo.UseShellExecute = false;
			processStartInfo.CreateNoWindow = true;
			process.StartInfo = processStartInfo;
			process.Start();
			while (!process.HasExited)
			{
				while (process.StandardOutput.Peek() >= 0)
				{
					activeProcesses.Add(process.StandardOutput.ReadLine());
				}
			}
		}
		catch (Exception)
		{
			activeProcesses.Add("/");
		}
	}

	protected override void ECOKEOMFPDB()
	{
	}

	protected override void ONIMOIKCHNA()
	{
	}

	protected override void LKCDALEJDOA()
	{
		activeProcesses = new List<string>();
		try
		{
			Process process = new Process();
			ProcessStartInfo processStartInfo = new ProcessStartInfo();
			processStartInfo.WindowStyle = ProcessWindowStyle.Hidden;
			processStartInfo.FileName = "Set EnvSprite Image";
			processStartInfo.Arguments = "error";
			processStartInfo.Verb = "InfoText";
			processStartInfo.RedirectStandardOutput = true;
			processStartInfo.RedirectStandardError = false;
			processStartInfo.UseShellExecute = false;
			processStartInfo.CreateNoWindow = false;
			process.StartInfo = processStartInfo;
			process.Start();
			while (!process.HasExited)
			{
				while (process.StandardOutput.Peek() >= 1)
				{
					activeProcesses.Add(process.StandardOutput.ReadLine());
				}
			}
		}
		catch (Exception)
		{
			activeProcesses.Add("_Distance");
		}
	}

	protected override void FBGOBKEFCKL()
	{
		activeProcesses = new List<string>();
		try
		{
			Process process = new Process();
			ProcessStartInfo processStartInfo = new ProcessStartInfo();
			processStartInfo.WindowStyle = ProcessWindowStyle.Normal;
			processStartInfo.FileName = " PhotonView: ";
			processStartInfo.Arguments = "_HighlightSuppression";
			processStartInfo.Verb = "_MainTex2";
			processStartInfo.RedirectStandardOutput = false;
			processStartInfo.RedirectStandardError = true;
			processStartInfo.UseShellExecute = false;
			processStartInfo.CreateNoWindow = false;
			process.StartInfo = processStartInfo;
			process.Start();
			while (!process.HasExited)
			{
				while (process.StandardOutput.Peek() >= 1)
				{
					activeProcesses.Add(process.StandardOutput.ReadLine());
				}
			}
		}
		catch (Exception)
		{
			activeProcesses.Add(" canvas not found");
		}
	}

	protected override void DFJHEJCCJGI()
	{
	}

	protected override void FHHKGGNEKNL()
	{
	}

	protected override void LBEAJCPDMJE()
	{
	}
}
