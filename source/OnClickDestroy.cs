// OnClickDestroy
using System.Collections;
using Photon;
using UnityEngine;

[RequireComponent(typeof(PhotonView))]
public class OnClickDestroy : Photon.MonoBehaviour
{
	public bool DestroyByRpc;

	public IEnumerator GNJLALNBBND()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public IEnumerator CGINAMFMMMF()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public void KBOKCPHKCMI()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			IJBFILBDGDO().RPC("_Green_R", (PhotonTargets)8, new object[1]);
		}
	}

	public IEnumerator DFEBIADHDKE()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public void CLPNGMEGNOC()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			GMAHNPNHMFK().RPC("_ScreenResolution", (PhotonTargets)7, new object[1]);
		}
	}

	public IEnumerator EPNPJGCKHOA()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public void OCPFHCPEFCM()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			AAMNKPHHHCI().RPC("Sprite", PhotonTargets.AllViaServer, new object[1]);
		}
	}

	public IEnumerator JKPOCCGKMCE()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public IEnumerator IGCIIGBOBAH()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public IEnumerator MFNHGIMNODN()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public IEnumerator CCCKPENCOAM()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public void NLGGGEBBFAH()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			JIOCGDBKGIL().RPC("No child content found, exiting..", (PhotonTargets)7, new object[1]);
		}
	}

	public IEnumerator AMCCDFECHOD()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public IEnumerator OKDBDPLBJNA()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public IEnumerator HLHECDHAOOE()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public IEnumerator PNNHIAPIFMJ()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public IEnumerator MDKFKKELHFB()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public IEnumerator KPCMLGOOHEP()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public IEnumerator HHPJECKENIC()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public IEnumerator CBALCEJEJAB()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public IEnumerator GJFFGEDCGMI()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public void IMNOHPHOEMN()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			CIACEFBNDDJ().RPC("#alreadyexist", PhotonTargets.AllBufferedViaServer, new object[1]);
		}
	}

	public IEnumerator EDOOBNEKGEN()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public void JDNMFCFIDEM()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			EOOCGIFFKBG().RPC("float,1.5", PhotonTargets.OthersBuffered);
		}
	}

	public void NEMCAJDNOPE()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			base.photonView.RPC("[CraftingPanel] Init", (PhotonTargets)7, new object[1]);
		}
	}

	public IEnumerator KIINIJCAGGA()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public void LAEHEFEDEAK()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			GMAHNPNHMFK().RPC("addNewButton", PhotonTargets.AllViaServer, new object[1]);
		}
	}

	public void IIPAHIBFIHH()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			CIACEFBNDDJ().RPC(".lastCheckpoint.correctScore", PhotonTargets.MasterClient);
		}
	}

	public IEnumerator KFPPAKHLILA()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public IEnumerator OCLECAMPIKK()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public IEnumerator PNPHPHOGCBC()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public void HJICAJKELGI()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			JIOCGDBKGIL().RPC("_NeighbourMaxTex", PhotonTargets.OthersBuffered);
		}
	}

	public IEnumerator OGDNFECEMHF()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public void CJBCKPEOOGI()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			CIACEFBNDDJ().RPC(":", PhotonTargets.AllViaServer);
		}
	}

	public IEnumerator HOLGLABGGJD()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public void GHDFEDMLPCB()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			ACPGHFHDCIP().RPC("_ScreenResolution", PhotonTargets.AllBuffered);
		}
	}

	public IEnumerator EJGIAIIJGJJ()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public void OnClick()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			base.photonView.RPC("DestroyRpc", PhotonTargets.AllBuffered);
		}
	}

	public void NMOMMMPKMEL()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			IFENGKHOKPC().RPC("maxScore", PhotonTargets.MasterClient, new object[1]);
		}
	}

	public void ALKAFKPMGIK()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			base.photonView.RPC("ERROR You should never change PhotonPlayer IDs!", PhotonTargets.AllBuffered);
		}
	}

	public void GLMCDMOHOKD()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			JAEJDHHCJJO().RPC("<b>#", PhotonTargets.AllBufferedViaServer, new object[1]);
		}
	}

	public void PNFNAPLBGIN()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			DJHCJIJAIJP().RPC("caret", PhotonTargets.OthersBuffered, new object[1]);
		}
	}

	public void KHLOKBDJLAO()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			EOOCGIFFKBG().RPC("PLEASE WAIT", PhotonTargets.All);
		}
	}

	[PunRPC]
	public IEnumerator DestroyRpc()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public void AKBAPOMKFPE()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			GBMJAPGLMGB().RPC("PLAY [SPACE]", PhotonTargets.AllViaServer);
		}
	}

	public void DDLBJCGMACI()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			ACPGHFHDCIP().RPC("offsets", PhotonTargets.AllViaServer, new object[1]);
		}
	}

	public IEnumerator LAONNHPOIKE()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public void EIDIMHEPOLO()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			OELHGNKAMEG().RPC("EnableRankedNotificationsToggle", PhotonTargets.AllViaServer);
		}
	}

	public void HHAECEMFKND()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			ACPGHFHDCIP().RPC("SetRoomStartTimestamp", PhotonTargets.AllBufferedViaServer);
		}
	}

	public IEnumerator IBADAHHDCBG()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public void NECOKPEMDKP()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			OELHGNKAMEG().RPC(" PhotonNetwork.networkingPeer.AvailableRegions ", PhotonTargets.All);
		}
	}

	public void CJFHNNICLEN()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			EOOCGIFFKBG().RPC("CameraFilterPack/Gradients_Therma", (PhotonTargets)7);
		}
	}

	public void MPELHKFCCFA()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			AAMNKPHHHCI().RPC("AudioClip", PhotonTargets.AllBuffered, new object[1]);
		}
	}

	public void MOHGKNDJFFL()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			AAMNKPHHHCI().RPC("It seems some instantiation is not completed, as instantiation data is used. You should make sure instantiations are paused when calling this method. Cleaning now, despite this.", (PhotonTargets)8);
		}
	}

	public IEnumerator AFFGALADNLM()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public void NNPGOODLNEO()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			PHCBHAFMDEJ().RPC("SpeedSlider", (PhotonTargets)8, new object[1]);
		}
	}

	public void HPKNKMOCCHJ()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			OELHGNKAMEG().RPC("[NetworkScene] Exited", PhotonTargets.OthersBuffered);
		}
	}

	public IEnumerator HFHIAAFJOHI()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public void EHGHDMHFGKJ()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			GMAHNPNHMFK().RPC("settings.volume.sfx", PhotonTargets.AllBufferedViaServer, new object[1]);
		}
	}

	public void DAPLLJDMJCK()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			OELHGNKAMEG().RPC("_Value", PhotonTargets.All);
		}
	}

	public IEnumerator BACDEIEBLED()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public void LAOCNFBGEDE()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			JIOCGDBKGIL().RPC(".lastCheckpoint.time", (PhotonTargets)8);
		}
	}

	public void MCFNPLNJKBP()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			AAMNKPHHHCI().RPC("_MainTex2", PhotonTargets.Others, new object[1]);
		}
	}

	public IEnumerator FPJOMJALKEP()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public void MBPKLNFHAFH()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			JAEJDHHCJJO().RPC("StartButton", PhotonTargets.Others, new object[1]);
		}
	}

	public IEnumerator JNEEKABMLBC()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public void DBDCIAIJOKG()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			GBHNDHLAJLI().RPC("CameraFilterPack/Blend2Camera_Difference", PhotonTargets.Others);
		}
	}

	public void GGCLKOJPPDD()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			OELHGNKAMEG().RPC("0.00", PhotonTargets.Others);
		}
	}

	public void MILJCGDAPKA()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			BBGIDKNEGHD().RPC("NewMusicFileSelector", PhotonTargets.AllViaServer, new object[1]);
		}
	}

	public IEnumerator JABKLNDKJBD()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public void LJKOEIMJMMD()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			FFIPLLNKGEN().RPC("settings.shaders", PhotonTargets.MasterClient);
		}
	}

	public IEnumerator NNMLKBEKBAG()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public IEnumerator LKBOCMIENGP()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public void OLHMGFBCENO()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			GBHNDHLAJLI().RPC("MultiplayerButton", PhotonTargets.OthersBuffered, new object[1]);
		}
	}

	public IEnumerator PKHDCLMCFKI()
	{
		Object.Destroy(base.gameObject);
		yield return 0;
		PhotonNetwork.UnAllocateViewID(base.photonView.viewID);
	}

	public void EHBMHKDHAEG()
	{
		if (!DestroyByRpc)
		{
			PhotonNetwork.Destroy(base.gameObject);
		}
		else
		{
			IJBFILBDGDO().RPC("_Near", PhotonTargets.MasterClient);
		}
	}
}
