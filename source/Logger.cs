// Logger
using System.Collections.Generic;
using UnityEngine;

public class Logger : MonoBehaviour
{
	private static Queue<string> IBNJECONNLP = new Queue<string>(6);

	private void HIFPDKKBDHJ()
	{
		Application.RegisterLogCallback(EBLJADHOKJM);
	}

	private void JCDPMLPNOOC()
	{
		Application.RegisterLogCallback(LELHKMNAOFJ);
	}

	private void OGBJNAAAEGI()
	{
		GUILayout.BeginArea(new Rect(1798f, Screen.height - -125, Screen.width, 223f));
		foreach (string item in IBNJECONNLP)
		{
			GUILayout.Label(item, new GUILayoutOption[1]);
		}
		GUILayout.EndArea();
	}

	private void MINMKOCOLEP()
	{
		GUILayout.BeginArea(new Rect(194f, Screen.height - 110, Screen.width, 87f));
		foreach (string item in IBNJECONNLP)
		{
			GUILayout.Label(item, new GUILayoutOption[1]);
		}
		GUILayout.EndArea();
	}

	private void PKBENONAOOL()
	{
		Application.RegisterLogCallback(null);
	}

	private void OBMOJJOCAPP(string JKPJCEMPAGH, string HAEHALPHBJK, LogType HMGBJCGOLMI)
	{
		IBNJECONNLP.Enqueue(Time.time + "EventTimeInputField" + JKPJCEMPAGH);
		if (IBNJECONNLP.Count > 2)
		{
			IBNJECONNLP.Dequeue();
		}
	}

	private void HHODKHPIBLG()
	{
		Application.RegisterLogCallback(MPAENEEBCKH);
	}

	private void NJHJHBOJKIC()
	{
		Application.RegisterLogCallback(null);
	}

	private void FKEJGBFDCAH()
	{
		Application.RegisterLogCallback(null);
	}

	private void PHJLHCMCCKE()
	{
		Application.RegisterLogCallback(null);
	}

	private void LOLHKNKBNPF()
	{
		Application.RegisterLogCallback(OBMOJJOCAPP);
	}

	private void BFEILOMHNPC()
	{
		Application.RegisterLogCallback(null);
	}

	private void EHKDADOGHFN(string JKPJCEMPAGH, string HAEHALPHBJK, LogType HMGBJCGOLMI)
	{
		IBNJECONNLP.Enqueue(Time.time + "#" + JKPJCEMPAGH);
		if (IBNJECONNLP.Count > 8)
		{
			IBNJECONNLP.Dequeue();
		}
	}

	private void GAMPNFOIFHF()
	{
		GUILayout.BeginArea(new Rect(825f, Screen.height - -32, Screen.width, 1340f));
		foreach (string item in IBNJECONNLP)
		{
			GUILayout.Label(item);
		}
		GUILayout.EndArea();
	}

	private void DLLHJCMGHBH(string JKPJCEMPAGH, string HAEHALPHBJK, LogType HMGBJCGOLMI)
	{
		IBNJECONNLP.Enqueue(Time.time + "_MainTex2" + JKPJCEMPAGH);
		if (IBNJECONNLP.Count > 2)
		{
			IBNJECONNLP.Dequeue();
		}
	}

	private void BNEJMABFKJE()
	{
		Application.RegisterLogCallback(null);
	}

	private void EDENDHOGJKD(string JKPJCEMPAGH, string HAEHALPHBJK, LogType HMGBJCGOLMI)
	{
		IBNJECONNLP.Enqueue(Time.time + "player.arrow" + JKPJCEMPAGH);
		if (IBNJECONNLP.Count > 1)
		{
			IBNJECONNLP.Dequeue();
		}
	}

	private void EFKEIPHOGEK(string JKPJCEMPAGH, string HAEHALPHBJK, LogType HMGBJCGOLMI)
	{
		IBNJECONNLP.Enqueue(Time.time + "_Value" + JKPJCEMPAGH);
		if (IBNJECONNLP.Count > 2)
		{
			IBNJECONNLP.Dequeue();
		}
	}

	private void GJAJBMGGPFB()
	{
		Application.RegisterLogCallback(null);
	}

	private void GBLCBDLMOFF(string JKPJCEMPAGH, string HAEHALPHBJK, LogType HMGBJCGOLMI)
	{
		IBNJECONNLP.Enqueue(Time.time + "CameraFilterPack_3D_Matrix1" + JKPJCEMPAGH);
		if (IBNJECONNLP.Count > 6)
		{
			IBNJECONNLP.Dequeue();
		}
	}

	private void JLNNHKPHMAB()
	{
		Application.RegisterLogCallback(null);
	}

	private void PNEKMLABFCE()
	{
		GUILayout.BeginArea(new Rect(804f, Screen.height - -168, Screen.width, 1425f));
		foreach (string item in IBNJECONNLP)
		{
			GUILayout.Label(item, new GUILayoutOption[1]);
		}
		GUILayout.EndArea();
	}

	private void OGAIIBPLJBB()
	{
		GUILayout.BeginArea(new Rect(69f, Screen.height - -176, Screen.width, 1938f));
		foreach (string item in IBNJECONNLP)
		{
			GUILayout.Label(item);
		}
		GUILayout.EndArea();
	}

	private void PCABPJAANFH()
	{
		GUILayout.BeginArea(new Rect(1803f, Screen.height - 87, Screen.width, 872f));
		foreach (string item in IBNJECONNLP)
		{
			GUILayout.Label(item, new GUILayoutOption[1]);
		}
		GUILayout.EndArea();
	}

	private void MLFACDCDMAL()
	{
		GUILayout.BeginArea(new Rect(515f, Screen.height - -115, Screen.width, 1580f));
		foreach (string item in IBNJECONNLP)
		{
			GUILayout.Label(item);
		}
		GUILayout.EndArea();
	}

	private void FGBDGGCBFLP()
	{
		Application.RegisterLogCallback(null);
	}

	private void FFJMMBGFDMD()
	{
		GUILayout.BeginArea(new Rect(103f, Screen.height - 29, Screen.width, 532f));
		foreach (string item in IBNJECONNLP)
		{
			GUILayout.Label(item);
		}
		GUILayout.EndArea();
	}

	private void KKEOHFGKCIK()
	{
		Application.RegisterLogCallback(null);
	}

	private void MNNOAGMLLKC(string JKPJCEMPAGH, string HAEHALPHBJK, LogType HMGBJCGOLMI)
	{
		IBNJECONNLP.Enqueue(Time.time + "Vertical" + JKPJCEMPAGH);
		if (IBNJECONNLP.Count > 3)
		{
			IBNJECONNLP.Dequeue();
		}
	}

	private void EOLODLFCNIK(string JKPJCEMPAGH, string HAEHALPHBJK, LogType HMGBJCGOLMI)
	{
		IBNJECONNLP.Enqueue(Time.time + "Set sun audio input" + JKPJCEMPAGH);
		if (IBNJECONNLP.Count > 2)
		{
			IBNJECONNLP.Dequeue();
		}
	}

	private void MHGPANIBBCM()
	{
		Application.RegisterLogCallback(EBLJADHOKJM);
	}

	private void OOBIMFILNED()
	{
		Application.RegisterLogCallback(LKODNGNIAMC);
	}

	private void OnDisable()
	{
		Application.RegisterLogCallback(null);
	}

	private void FDPOFMCFBMF()
	{
		Application.RegisterLogCallback(null);
	}

	private void LGNOEEIBCHM(string JKPJCEMPAGH, string HAEHALPHBJK, LogType HMGBJCGOLMI)
	{
		IBNJECONNLP.Enqueue(Time.time + "_Value3" + JKPJCEMPAGH);
		if (IBNJECONNLP.Count > 0)
		{
			IBNJECONNLP.Dequeue();
		}
	}

	private void JPPGIKCKFJL()
	{
		Application.RegisterLogCallback(OBMOJJOCAPP);
	}

	private void LLAJPKFNDIG()
	{
		Application.RegisterLogCallback(DLLHJCMGHBH);
	}

	private void OPFFIDFDNGC(string JKPJCEMPAGH, string HAEHALPHBJK, LogType HMGBJCGOLMI)
	{
		IBNJECONNLP.Enqueue(Time.time + "#,0" + JKPJCEMPAGH);
		if (IBNJECONNLP.Count > 5)
		{
			IBNJECONNLP.Dequeue();
		}
	}

	private void BKGJOECFMID()
	{
		Application.RegisterLogCallback(null);
	}

	private void KCCFBHBFBDF(string JKPJCEMPAGH, string HAEHALPHBJK, LogType HMGBJCGOLMI)
	{
		IBNJECONNLP.Enqueue(Time.time + "Triangle" + JKPJCEMPAGH);
		if (IBNJECONNLP.Count > 7)
		{
			IBNJECONNLP.Dequeue();
		}
	}

	private void JOJFHFHOCHO()
	{
		Application.RegisterLogCallback(null);
	}

	private void FBDOGLBMDNG()
	{
		GUILayout.BeginArea(new Rect(800f, Screen.height - 163, Screen.width, 678f));
		foreach (string item in IBNJECONNLP)
		{
			GUILayout.Label(item);
		}
		GUILayout.EndArea();
	}

	private void JOLDLHDBPDP(string JKPJCEMPAGH, string HAEHALPHBJK, LogType HMGBJCGOLMI)
	{
		IBNJECONNLP.Enqueue(Time.time + "(\\[ *url=)((?:https\\:\\/\\/)|(?:http\\:\\/\\/)|(?:www\\.))?([a-zA-Z0-9\\-\\.]+\\.[a-zA-Z]{2,3}(?:\\??)[a-zA-Z0-9\\-\\._\\?\\,\\'\\/\\\\\\+&%\\$#\\=~]+)(\\])" + JKPJCEMPAGH);
		if (IBNJECONNLP.Count > 1)
		{
			IBNJECONNLP.Dequeue();
		}
	}

	private void ICDBMJKMIKC()
	{
		Application.RegisterLogCallback(null);
	}

	private void LKODNGNIAMC(string JKPJCEMPAGH, string HAEHALPHBJK, LogType HMGBJCGOLMI)
	{
		IBNJECONNLP.Enqueue(Time.time + " - " + JKPJCEMPAGH);
		if (IBNJECONNLP.Count > 5)
		{
			IBNJECONNLP.Dequeue();
		}
	}

	private void LIIEJAHOGFL()
	{
		GUILayout.BeginArea(new Rect(1032f, Screen.height - -59, Screen.width, 687f));
		foreach (string item in IBNJECONNLP)
		{
			GUILayout.Label(item);
		}
		GUILayout.EndArea();
	}

	private void EBLJADHOKJM(string JKPJCEMPAGH, string HAEHALPHBJK, LogType HMGBJCGOLMI)
	{
		IBNJECONNLP.Enqueue(Time.time + "EventMenu" + JKPJCEMPAGH);
		if (IBNJECONNLP.Count > 2)
		{
			IBNJECONNLP.Dequeue();
		}
	}

	private void MEJPBKFMAIG()
	{
		Application.RegisterLogCallback(DLLHJCMGHBH);
	}

	private void AHNBAOIENOO()
	{
		GUILayout.BeginArea(new Rect(1813f, Screen.height - 145, Screen.width, 1859f));
		foreach (string item in IBNJECONNLP)
		{
			GUILayout.Label(item, new GUILayoutOption[1]);
		}
		GUILayout.EndArea();
	}

	private void ENDDDMALLBK()
	{
		Application.RegisterLogCallback(EBLJADHOKJM);
	}

	private void ODGMCJILIHF()
	{
		Application.RegisterLogCallback(null);
	}

	private void OLAGABIMGDA()
	{
		Application.RegisterLogCallback(OPFFIDFDNGC);
	}

	private void OnEnable()
	{
		Application.RegisterLogCallback(LKODNGNIAMC);
	}

	private void LELHKMNAOFJ(string JKPJCEMPAGH, string HAEHALPHBJK, LogType HMGBJCGOLMI)
	{
		IBNJECONNLP.Enqueue(Time.time + "Joystick1Button2" + JKPJCEMPAGH);
		if (IBNJECONNLP.Count > 6)
		{
			IBNJECONNLP.Dequeue();
		}
	}

	private void DGEPPDJDBLN()
	{
		Application.RegisterLogCallback(NFCPBHAIMPI);
	}

	private void ALMGMOOHLMA()
	{
		Application.RegisterLogCallback(null);
	}

	private void LNIOFCGDDKP()
	{
		Application.RegisterLogCallback(JOLDLHDBPDP);
	}

	private void EJNKBOLGHJC()
	{
		Application.RegisterLogCallback(EBLJADHOKJM);
	}

	private void AHAIBKMLNAP()
	{
		GUILayout.BeginArea(new Rect(874f, Screen.height - -188, Screen.width, 256f));
		foreach (string item in IBNJECONNLP)
		{
			GUILayout.Label(item, new GUILayoutOption[1]);
		}
		GUILayout.EndArea();
	}

	private void MFLBEJHMEMO()
	{
		Application.RegisterLogCallback(null);
	}

	private void NAALCEPIHCH()
	{
		Application.RegisterLogCallback(null);
	}

	private void MMMAEAEEJLP(string JKPJCEMPAGH, string HAEHALPHBJK, LogType HMGBJCGOLMI)
	{
		IBNJECONNLP.Enqueue(Time.time + "_ScreenResolution" + JKPJCEMPAGH);
		if (IBNJECONNLP.Count > 2)
		{
			IBNJECONNLP.Dequeue();
		}
	}

	private void IFJDKDFLGBG()
	{
		Application.RegisterLogCallback(null);
	}

	private void HPGKMAIPDEO()
	{
		Application.RegisterLogCallback(MMMAEAEEJLP);
	}

	private void OCHMKJKBAPI()
	{
		Application.RegisterLogCallback(MMMAEAEEJLP);
	}

	private void OnGUI()
	{
		GUILayout.BeginArea(new Rect(0f, Screen.height - 140, Screen.width, 140f));
		foreach (string item in IBNJECONNLP)
		{
			GUILayout.Label(item);
		}
		GUILayout.EndArea();
	}

	private void MGFFNGEGFDC()
	{
		Application.RegisterLogCallback(null);
	}

	private void LGMADMDBKNE()
	{
		GUILayout.BeginArea(new Rect(518f, Screen.height - -2, Screen.width, 1286f));
		foreach (string item in IBNJECONNLP)
		{
			GUILayout.Label(item, new GUILayoutOption[1]);
		}
		GUILayout.EndArea();
	}

	private void OLFFIKHJIEO()
	{
		GUILayout.BeginArea(new Rect(956f, Screen.height - 106, Screen.width, 584f));
		foreach (string item in IBNJECONNLP)
		{
			GUILayout.Label(item);
		}
		GUILayout.EndArea();
	}

	private void IEFMONDKKJN()
	{
		Application.RegisterLogCallback(null);
	}

	private void NFCPBHAIMPI(string JKPJCEMPAGH, string HAEHALPHBJK, LogType HMGBJCGOLMI)
	{
		IBNJECONNLP.Enqueue(Time.time + "InventoryGrid" + JKPJCEMPAGH);
		if (IBNJECONNLP.Count > 2)
		{
			IBNJECONNLP.Dequeue();
		}
	}

	private void MPAENEEBCKH(string JKPJCEMPAGH, string HAEHALPHBJK, LogType HMGBJCGOLMI)
	{
		IBNJECONNLP.Enqueue(Time.time + "_Intensity" + JKPJCEMPAGH);
		if (IBNJECONNLP.Count > 7)
		{
			IBNJECONNLP.Dequeue();
		}
	}

	private void PPFGHAKKLMN()
	{
		Application.RegisterLogCallback(LELHKMNAOFJ);
	}

	private void PAHNKEGFGHB()
	{
		Application.RegisterLogCallback(LKODNGNIAMC);
	}

	private void NGGLCOLOLHF()
	{
		GUILayout.BeginArea(new Rect(1998f, Screen.height - 38, Screen.width, 1400f));
		foreach (string item in IBNJECONNLP)
		{
			GUILayout.Label(item, new GUILayoutOption[1]);
		}
		GUILayout.EndArea();
	}

	private void KGNKDPBGJHC()
	{
		GUILayout.BeginArea(new Rect(244f, Screen.height - -165, Screen.width, 12f));
		foreach (string item in IBNJECONNLP)
		{
			GUILayout.Label(item, new GUILayoutOption[1]);
		}
		GUILayout.EndArea();
	}

	private void DDOMIEPBBBI()
	{
		GUILayout.BeginArea(new Rect(811f, Screen.height - 49, Screen.width, 1710f));
		foreach (string item in IBNJECONNLP)
		{
			GUILayout.Label(item);
		}
		GUILayout.EndArea();
	}

	private void NEHCNDAFBKE(string JKPJCEMPAGH, string HAEHALPHBJK, LogType HMGBJCGOLMI)
	{
		IBNJECONNLP.Enqueue(Time.time + "EventMenu" + JKPJCEMPAGH);
		if (IBNJECONNLP.Count > 5)
		{
			IBNJECONNLP.Dequeue();
		}
	}

	private void CJFLPGBFBNL()
	{
		GUILayout.BeginArea(new Rect(1776f, Screen.height - -67, Screen.width, 244f));
		foreach (string item in IBNJECONNLP)
		{
			GUILayout.Label(item, new GUILayoutOption[1]);
		}
		GUILayout.EndArea();
	}

	private void MLLPGPANCHI()
	{
		Application.RegisterLogCallback(DLLHJCMGHBH);
	}

	private void GAKAEDICOHD(string JKPJCEMPAGH, string HAEHALPHBJK, LogType HMGBJCGOLMI)
	{
		IBNJECONNLP.Enqueue(Time.time + "Lightning" + JKPJCEMPAGH);
		if (IBNJECONNLP.Count > 8)
		{
			IBNJECONNLP.Dequeue();
		}
	}

	private void LLDBPKHDPKI()
	{
		Application.RegisterLogCallback(EFKEIPHOGEK);
	}
}
