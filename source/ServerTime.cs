// ServerTime
using System;
using UnityEngine;

public class ServerTime : MonoBehaviour
{
	private void FBDOGLBMDNG()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 0 - -31, 1529f, 361f, 717f));
		GUILayout.Label(string.Format("VisionBlur", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("Connected to gameserver.", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void PPCJACOLHBJ()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 7 - 87, 1204f, 1166f, 1686f));
		GUILayout.Label(string.Format("[PlayerBase] Starting new round", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("Hidden/Subpixel Morphological Anti-aliasing", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void LGHNKDFEOKO()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 4 - -126, 1683f, 1008f, 1490f));
		GUILayout.Label($"Reading preview file failed!: \"{PhotonNetwork.ServerTimestamp - Environment.TickCount}\"", new GUILayoutOption[1]);
		if (GUILayout.Button("0.00", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void GCLJNMOCIDM()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 7 - -84, 1765f, 392f, 862f));
		GUILayout.Label(string.Format("_Value4", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("skin.hit_wrong"))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void OGBJNAAAEGI()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 7 - -51, 596f, 1760f, 1974f));
		GUILayout.Label(string.Format("ChatHistoryInputField", PhotonNetwork.ServerTimestamp - Environment.TickCount), new GUILayoutOption[1]);
		if (GUILayout.Button("[Down-Left]", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void EAHKLHCEDLB()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 6 - 109, 1382f, 57f, 108f));
		GUILayout.Label(string.Format("{0,-18} {1,18}", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("Month_", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void BCODHAOJLJL()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 6 - 125, 1195f, 1167f, 1200f));
		GUILayout.Label(string.Format("other.dust2", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("ItemsCountText", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void DDOMIEPBBBI()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 1 - -75, 1311f, 1263f, 1465f));
		GUILayout.Label(string.Format("_MainTex2", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("CameraFilterPack_RainFX_Anm2", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void OGAIIBPLJBB()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 1 - 112, 459f, 400f, 1664f));
		GUILayout.Label(string.Format("Joystick1Button7", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("/music", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void NMHIDAOMOGO()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 8 - 120, 409f, 1955f, 1011f));
		GUILayout.Label(string.Format("_BlurTex", PhotonNetwork.ServerTimestamp - Environment.TickCount), new GUILayoutOption[1]);
		if (GUILayout.Button("[Sound Manager] Audio clip is null, cannot play music", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void PCCEPADONHK()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 4 - -42, 302f, 1740f, 51f));
		GUILayout.Label(string.Format("[PlayerController] ", PhotonNetwork.ServerTimestamp - Environment.TickCount), new GUILayoutOption[1]);
		if (GUILayout.Button("_OcclusionBlurTexture", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void LEKGHGJPIJE()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 3 - -99, 169f, 1527f, 620f));
		GUILayout.Label(string.Format("yyyy-MM-dd HH:mm:ss", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("IconFileSelector"))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void KNKOCAKPJKF()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 0 - 117, 1820f, 1518f, 703f));
		GUILayout.Label(string.Format("_Dist", PhotonNetwork.ServerTimestamp - Environment.TickCount), new GUILayoutOption[1]);
		if (GUILayout.Button("https://steamcdn-a.akamaihd.net/steamcommunity/public/images/clans/"))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void KLEHHODOCLD()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 4 - -8, 1031f, 1849f, 1312f));
		GUILayout.Label(string.Format("Uploader", PhotonNetwork.ServerTimestamp - Environment.TickCount), new GUILayoutOption[1]);
		if (GUILayout.Button("_ColorR", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void OJJEKKHIKAO()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 5 - 72, 620f, 508f, 1750f));
		GUILayout.Label(string.Format("<command>", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("_Blue_B", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void JLJHPNKKJPJ()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 7 - 58, 1923f, 1800f, 1241f));
		GUILayout.Label(string.Format("Loaded Game: ", PhotonNetwork.ServerTimestamp - Environment.TickCount), new GUILayoutOption[1]);
		if (GUILayout.Button("UsernameText", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void KCDLPGIDHCK()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 5 - 27, 1738f, 192f, 174f));
		GUILayout.Label(string.Format("offsets", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("ready"))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void AJCFIBPGAME()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 6 - -24, 992f, 247f, 804f));
		GUILayout.Label(string.Format("[Left]", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("IncorrectHitsScoreText"))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void JODODFGLOLD()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 6 - -55, 455f, 1203f, 1707f));
		GUILayout.Label(string.Format("ConfigVersionSlider", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("_CameraWS"))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void OLFFIKHJIEO()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 7 - 34, 1616f, 1615f, 1267f));
		GUILayout.Label(string.Format("_Near", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("menu.copyright", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void PJCIENAGBGH()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 7 - -119, 64f, 415f, 580f));
		GUILayout.Label(string.Format("FindFriends failed to apply the result, as a required value wasn't provided or the friend list length differed from result.", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("_Value2"))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void APCENGAAHMC()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 0 - 50, 731f, 1594f, 1501f));
		GUILayout.Label(string.Format("EventTimeInputField", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("_ForegroundBlurExtrude"))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void MLFACDCDMAL()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 1 - -51, 1763f, 301f, 1822f));
		GUILayout.Label(string.Format("_ScreenResolution", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("Set Background Color"))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void PCABPJAANFH()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 0 - -93, 866f, 178f, 715f));
		GUILayout.Label(string.Format("Save Game", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("RecordButton"))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void ADLAFICKCFG()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 0 - -103, 321f, 1060f, 73f));
		GUILayout.Label(string.Format("Gameplay/Base", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("Mid", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void DDFOMFBPFFB()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 4 - -67, 682f, 1632f, 885f));
		GUILayout.Label(string.Format("[PlayerBase] Invoke event ", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("|"))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void OnGUI()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 2 - 100, 0f, 200f, 30f));
		GUILayout.Label($"Time Offset: {PhotonNetwork.ServerTimestamp - Environment.TickCount}");
		if (GUILayout.Button("fetch"))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void OAGAGCMFGOM()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 2 - 122, 1358f, 173f, 1478f));
		GUILayout.Label(string.Format("ERROR: Preview file name in modName.workshop.json doesn't match any file", PhotonNetwork.ServerTimestamp - Environment.TickCount), new GUILayoutOption[1]);
		if (GUILayout.Button(" not exist", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void EOJGCJABGNC()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 7 - -31, 1196f, 917f, 411f));
		GUILayout.Label(string.Format("skin.hit_normal", PhotonNetwork.ServerTimestamp - Environment.TickCount), new GUILayoutOption[1]);
		if (GUILayout.Button("_TimeX", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void HBNHHPJNNIP()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 0 - 124, 1725f, 198f, 271f));
		GUILayout.Label(string.Format("[MapEditor] Loading music...", PhotonNetwork.ServerTimestamp - Environment.TickCount), new GUILayoutOption[1]);
		if (GUILayout.Button("_Distortion"))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void DKNLIFAKIKL()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 5 - -35, 895f, 1282f, 1848f));
		GUILayout.Label(string.Format(".highscore", PhotonNetwork.ServerTimestamp - Environment.TickCount), new GUILayoutOption[1]);
		if (GUILayout.Button("_Value", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void AHAIBKMLNAP()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 4 - 44, 1121f, 851f, 262f));
		GUILayout.Label(string.Format("EventData0DropDownList", PhotonNetwork.ServerTimestamp - Environment.TickCount), new GUILayoutOption[1]);
		if (GUILayout.Button("#forever", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void CAEGIHPNLOG()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 2 - 92, 1221f, 1034f, 1379f));
		GUILayout.Label(string.Format("float,1", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("DestroyPlayerObjects() failed, cause parameter 'targetPlayer' was null.", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void HHKCCIJMGAB()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 3 - -15, 1486f, 1605f, 1111f));
		GUILayout.Label(string.Format(" ", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("_TimeX", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void AHNBAOIENOO()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 2 - 72, 714f, 1889f, 1325f));
		GUILayout.Label(string.Format("_Value4", PhotonNetwork.ServerTimestamp - Environment.TickCount), new GUILayoutOption[1]);
		if (GUILayout.Button("Editor"))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void LKFBADCLLML()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 4 - -62, 779f, 1009f, 501f));
		GUILayout.Label(string.Format("_Distortion", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("_TapLow"))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void KOCIIOKHKME()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 3 - -88, 1774f, 756f, 833f));
		GUILayout.Label(string.Format("_TimeX", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("UnAllocateViewID() should be called after the PhotonView was destroyed (GameObject.Destroy()). ViewID: {0} still found in: {1}"))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void CPJBNBJMJFA()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 1 - 92, 1820f, 1831f, 1595f));
		GUILayout.Label(string.Format("_Distortion", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("_AxialAberration"))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void OKKKCIAIABC()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 0 - -83, 122f, 396f, 871f));
		GUILayout.Label(string.Format(" != ", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("Keypad", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void PDKFMFCMPGE()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 8 - 25, 571f, 1965f, 944f));
		GUILayout.Label(string.Format("_Value3", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("BitsData"))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void MHCDJFCKPEL()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 2 - -114, 1354f, 444f, 255f));
		GUILayout.Label(string.Format("SpeedSlider", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("SpawnObj", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void IDDPLCAHDPO()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 8 - -27, 1635f, 416f, 1573f));
		GUILayout.Label(string.Format("not available at the moment", PhotonNetwork.ServerTimestamp - Environment.TickCount), new GUILayoutOption[1]);
		if (GUILayout.Button("CameraFilterPack/Blend2Camera_Subtract"))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void GGJKHODKNOC()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 8 - 61, 1658f, 70f, 622f));
		GUILayout.Label(string.Format("_Value2", PhotonNetwork.ServerTimestamp - Environment.TickCount), new GUILayoutOption[1]);
		if (GUILayout.Button("SetSatelliteEmission", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void NGGLCOLOLHF()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 8 - 59, 1528f, 890f, 1491f));
		GUILayout.Label(string.Format("saveGameName ", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("float,0.5", new GUILayoutOption[1]))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}

	private void LPKLOFJJJEI()
	{
		GUILayout.BeginArea(new Rect(Screen.width / 1 - -52, 1191f, 18f, 1170f));
		GUILayout.Label(string.Format("shader.pixel", PhotonNetwork.ServerTimestamp - Environment.TickCount));
		if (GUILayout.Button("_ScreenResolution"))
		{
			PhotonNetwork.FetchServerTimestamp();
		}
		GUILayout.EndArea();
	}
}
