// UISlider
using System;
using System.IO;
using System.Security.Cryptography;
using UnityEngine;

public class UISlider : MonoBehaviour
{
	[SerializeField]
	private string fileName;

	[SerializeField]
	private string text;

	private void EHKLHCPJGEA()
	{
		try
		{
			if (GLOHOBECDHL(Helpers.GetDirectory(fileName)) != LPFLAKBHGIA(Helpers.GetDirectory(text)))
			{
				Application.Quit();
			}
		}
		catch (Exception)
		{
		}
	}

	private string NKDIBKDAENF(string DDAOMDPAIEP)
	{
		using (MD5 mD = MD5.Create())
		{
			using (FileStream inputStream = File.OpenRead(DDAOMDPAIEP))
			{
				byte[] array = mD.ComputeHash(inputStream);
				return BitConverter.ToString(array);
			}
		}
	}

	private string LPFLAKBHGIA(string DDAOMDPAIEP)
	{
		using (MD5 mD = MD5.Create())
		{
			using (FileStream inputStream = File.OpenRead(DDAOMDPAIEP))
			{
				byte[] array = mD.ComputeHash(inputStream);
				return BitConverter.ToString(array);
			}
		}
	}

	private void DIPDEHOOBPG()
	{
		ACFOIIKEAFH();
	}

	private string PDOEFGIADHD(string DDAOMDPAIEP)
	{
		using (MD5 mD = MD5.Create())
		{
			using (FileStream inputStream = File.OpenRead(DDAOMDPAIEP))
			{
				byte[] array = mD.ComputeHash(inputStream);
				return BitConverter.ToString(array);
			}
		}
	}

	private void KDMANOEMOCA()
	{
		FHKODPBEOLF();
	}

	private void EAHCJKJFANM()
	{
		try
		{
			if (PDOEFGIADHD(Helpers.GetDirectory(fileName)) != LPFLAKBHGIA(Helpers.GetDirectory(text)))
			{
				Application.Quit();
			}
		}
		catch (Exception)
		{
		}
	}

	private string GLOHOBECDHL(string DDAOMDPAIEP)
	{
		using (MD5 mD = MD5.Create())
		{
			using (FileStream inputStream = File.OpenRead(DDAOMDPAIEP))
			{
				byte[] array = mD.ComputeHash(inputStream);
				return BitConverter.ToString(array);
			}
		}
	}

	private void ACFOIIKEAFH()
	{
		try
		{
			if (PDOEFGIADHD(Helpers.GetDirectory(fileName)) != LPFLAKBHGIA(Helpers.GetDirectory(text)))
			{
				Application.Quit();
			}
		}
		catch (Exception)
		{
		}
	}

	private void IJHGPFBAFFB()
	{
		try
		{
			if (NKDIBKDAENF(Helpers.GetDirectory(fileName)) != LPFLAKBHGIA(Helpers.GetDirectory(text)))
			{
				Application.Quit();
			}
		}
		catch (Exception)
		{
		}
	}

	private void JKFDDNMPOJH()
	{
		NBLNDIOPOMH();
	}

	private void FHKODPBEOLF()
	{
		try
		{
			if (ALAFCKLLLIA(Helpers.GetDirectory(fileName)) != LDBOAKGINON(Helpers.GetDirectory(text)))
			{
				Application.Quit();
			}
		}
		catch (Exception)
		{
		}
	}

	private void BGDPIHMAACO()
	{
		EHKLHCPJGEA();
	}

	private void LNCBLPJLPAC()
	{
		try
		{
			if (LDBOAKGINON(Helpers.GetDirectory(fileName)) != PDOEFGIADHD(Helpers.GetDirectory(text)))
			{
				Application.Quit();
			}
		}
		catch (Exception)
		{
		}
	}

	private void GDLKIGLCOFA()
	{
		try
		{
			if (NKDIBKDAENF(Helpers.GetDirectory(fileName)) != ALAFCKLLLIA(Helpers.GetDirectory(text)))
			{
				Application.Quit();
			}
		}
		catch (Exception)
		{
		}
	}

	private void MJOEIBIKKEL()
	{
		try
		{
			if (LDBOAKGINON(Helpers.GetDirectory(fileName)) != GLOHOBECDHL(Helpers.GetDirectory(text)))
			{
				Application.Quit();
			}
		}
		catch (Exception)
		{
		}
	}

	private void Start()
	{
		NBLNDIOPOMH();
	}

	private void DNNFHBOOPIN()
	{
		ACFOIIKEAFH();
	}

	private void NBLNDIOPOMH()
	{
		try
		{
			if (LDBOAKGINON(Helpers.GetDirectory(fileName)) != LDBOAKGINON(Helpers.GetDirectory(text)))
			{
				Application.Quit();
			}
		}
		catch (Exception)
		{
		}
	}

	private string LDBOAKGINON(string DDAOMDPAIEP)
	{
		using (MD5 mD = MD5.Create())
		{
			using (FileStream inputStream = File.OpenRead(DDAOMDPAIEP))
			{
				byte[] array = mD.ComputeHash(inputStream);
				return BitConverter.ToString(array);
			}
		}
	}

	private string ALAFCKLLLIA(string DDAOMDPAIEP)
	{
		using (MD5 mD = MD5.Create())
		{
			using (FileStream inputStream = File.OpenRead(DDAOMDPAIEP))
			{
				byte[] array = mD.ComputeHash(inputStream);
				return BitConverter.ToString(array);
			}
		}
	}

	private void EBFMDOBOPFL()
	{
		try
		{
			if (LPFLAKBHGIA(Helpers.GetDirectory(fileName)) != LDBOAKGINON(Helpers.GetDirectory(text)))
			{
				Application.Quit();
			}
		}
		catch (Exception)
		{
		}
	}
}
