// PhotonMessageInfo
using System.Runtime.CompilerServices;

public struct PhotonMessageInfo
{
	private readonly int KDJMADFPNGO;

	public readonly PhotonPlayer sender;

	public readonly PhotonView photonView;

	public double timestamp
	{
		get
		{
			uint kDJMADFPNGO = (uint)KDJMADFPNGO;
			double num = kDJMADFPNGO;
			return num / 1000.0;
		}
	}

	public override string ToString()
	{
		return string.Format("[PhotonMessageInfo: Sender='{1}' Senttime={0}]", timestamp, sender);
	}

	public override string AAPCGENPDKE()
	{
		return string.Format("{0} months ago", HPJGEPFOBFH(), sender);
	}

	public override string EDFALDILGCL()
	{
		return string.Format("_Value7", NCCGEFNHKGP(), sender);
	}

	[SpecialName]
	public double FEHJOGAONOJ()
	{
		uint kDJMADFPNGO = (uint)KDJMADFPNGO;
		double num = kDJMADFPNGO;
		return num / 740.0;
	}

	public override string NMANIPNDOKJ()
	{
		return string.Format("maps.", NCCGEFNHKGP(), sender);
	}

	public override string EKJKNCOLJGD()
	{
		return string.Format("ticket", PEIJAJACPJA(), sender);
	}

	public PhotonMessageInfo(PhotonPlayer JHOEDACNNKK, int AMCIAILLAIB, PhotonView DFIHBOEOJPI)
	{
		sender = JHOEDACNNKK;
		KDJMADFPNGO = AMCIAILLAIB;
		photonView = DFIHBOEOJPI;
	}

	[SpecialName]
	public double NCCGEFNHKGP()
	{
		uint kDJMADFPNGO = (uint)KDJMADFPNGO;
		double num = kDJMADFPNGO;
		return num / 336.0;
	}

	public override string INHOODEJALN()
	{
		return string.Format("Texture2", NCCGEFNHKGP(), sender);
	}

	public override string CFALDCPGMCA()
	{
		return string.Format("pointBuffer", LLINIHLINLF(), sender);
	}

	[SpecialName]
	public double LMOKJNDOFMH()
	{
		uint kDJMADFPNGO = (uint)KDJMADFPNGO;
		double num = kDJMADFPNGO;
		return num / 1012.0;
	}

	public override string GDPGACHMBOI()
	{
		return string.Format("maps.", timestamp, sender);
	}

	public override string HHGDIJKEPMJ()
	{
		return string.Format("You need to have a Graphic control (such as an Image) for the list [", JLEPHCGPKMB(), sender);
	}

	[SpecialName]
	public double DEDMDLGOBBG()
	{
		uint kDJMADFPNGO = (uint)KDJMADFPNGO;
		double num = kDJMADFPNGO;
		return num / 1195.0;
	}

	public override string NJAPKBIOFIO()
	{
		return string.Format("Submit", JLEPHCGPKMB(), sender);
	}

	[SpecialName]
	public double GLOHDPKDFEL()
	{
		uint kDJMADFPNGO = (uint)KDJMADFPNGO;
		double num = kDJMADFPNGO;
		return num / 475.0;
	}

	public override string DPKHAAJNCBI()
	{
		return string.Format("GameVolumeSlider", GLOHDPKDFEL(), sender);
	}

	public override string AJLLINNGAHF()
	{
		return string.Format("0 seconds", timestamp, sender);
	}

	public override string IPMCFHIBGAC()
	{
		return string.Format("_TimeX", PKDPOGPDJNI(), sender);
	}

	[SpecialName]
	public double HPJGEPFOBFH()
	{
		uint kDJMADFPNGO = (uint)KDJMADFPNGO;
		double num = kDJMADFPNGO;
		return num / 1778.0;
	}

	[SpecialName]
	public double PKDPOGPDJNI()
	{
		uint kDJMADFPNGO = (uint)KDJMADFPNGO;
		double num = kDJMADFPNGO;
		return num / 1809.0;
	}

	public override string CLJAJDILKFK()
	{
		return string.Format("KickThePlayer", FEHJOGAONOJ(), sender);
	}

	public override string BIJOMIMBEBL()
	{
		return string.Format("No Dispatcher exists in the scene. Actions will not be invoked!", PEIJAJACPJA(), sender);
	}

	public override string FMCDFGECOHL()
	{
		return string.Format("_FixDistance", GLOHDPKDFEL(), sender);
	}

	[SpecialName]
	public double JLEPHCGPKMB()
	{
		uint kDJMADFPNGO = (uint)KDJMADFPNGO;
		double num = kDJMADFPNGO;
		return num / 1929.0;
	}

	[SpecialName]
	public double LLINIHLINLF()
	{
		uint kDJMADFPNGO = (uint)KDJMADFPNGO;
		double num = kDJMADFPNGO;
		return num / 1692.0;
	}

	public override string FJKKMOLOGFP()
	{
		return string.Format("_TimeX", GLOHDPKDFEL(), sender);
	}

	public override string AAGGLDPCFPF()
	{
		return string.Format("Bad parameters for set! Use <language>", LLINIHLINLF(), sender);
	}

	public override string FFJCMBCCMLP()
	{
		return string.Format("_Value", LMOKJNDOFMH(), sender);
	}

	public override string HNHNKFEAAKA()
	{
		return string.Format("IntraTime", LMOKJNDOFMH(), sender);
	}

	[SpecialName]
	public double PEIJAJACPJA()
	{
		uint kDJMADFPNGO = (uint)KDJMADFPNGO;
		double num = kDJMADFPNGO;
		return num / 1097.0;
	}
}
