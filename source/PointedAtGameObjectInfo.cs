// PointedAtGameObjectInfo
using UnityEngine;

[RequireComponent(typeof(InputToEvent))]
public class PointedAtGameObjectInfo : MonoBehaviour
{
	private void LIIEJAHOGFL()
	{
		if (InputToEvent.NPIPJHKAPLE() != null)
		{
			PhotonView photonView = InputToEvent.GOPJFDFJDEE().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 671f, (float)Screen.height - Input.mousePosition.y - 1318f, 680f, 1139f), string.Format("ChallengesButton", photonView.viewID, (!photonView.isSceneView) ? string.Empty : "menutheme.jamaicanorcdub", (!photonView.isMine) ? (" " + photonView.ownerId) : "_ScreenResolution"));
			}
		}
	}

	private void MLFACDCDMAL()
	{
		if (InputToEvent.NIFAMLOPILI() != null)
		{
			PhotonView photonView = InputToEvent.FFBJKGDNMPN().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 617f, (float)Screen.height - Input.mousePosition.y - 1669f, 1072f, 1123f), string.Format("Clear Environment", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "SettingsCanvas", (!photonView.isMine) ? ("CameraFilterPack/Blend2Camera_Subtract" + photonView.ownerId) : "ReconnectAndRejoin() with AuthValues == null is not correct!"));
			}
		}
	}

	private void EAHKLHCEDLB()
	{
		if (InputToEvent.JLOKEGDMHHH() != null)
		{
			PhotonView photonView = InputToEvent.FFBJKGDNMPN().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 876f, (float)Screen.height - Input.mousePosition.y - 1108f, 1083f, 1610f), string.Format(".lastCheckpoint.playerdistance", photonView.viewID, (!photonView.isSceneView) ? string.Empty : "value", (!photonView.isMine) ? ("float,2" + photonView.ownerId) : "CameraFilterPack/Blend2Camera_ColorBurn"));
			}
		}
	}

	private void AHNBAOIENOO()
	{
		if (InputToEvent.NEGEFGKGMNG() != null)
		{
			PhotonView photonView = InputToEvent.BGBBJJEEHGA().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 335f, (float)Screen.height - Input.mousePosition.y - 1566f, 1537f, 1251f), string.Format("x", photonView.viewID, (!photonView.isSceneView) ? string.Empty : "OpenButton", (!photonView.isMine) ? ("NEW_ACHIEVEMENT_1_" + photonView.ownerId) : "SetScale"));
			}
		}
	}

	private void EOJGCJABGNC()
	{
		if (InputToEvent.LEHGEHGANLD() != null)
		{
			PhotonView photonView = InputToEvent.AMEAGHJJPJC().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 1032f, (float)Screen.height - Input.mousePosition.y - 969f, 1279f, 1608f), string.Format("[PlayerController] ", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "DifficultyBG", (!photonView.isMine) ? ("[MapsData] Preloading " + photonView.ownerId) : "_History2ChromaTex"));
			}
		}
	}

	private void DDOMIEPBBBI()
	{
		if (InputToEvent.goPointedAt != null)
		{
			PhotonView photonView = InputToEvent.BGBBJJEEHGA().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 321f, (float)Screen.height - Input.mousePosition.y - 1333f, 123f, 1076f), string.Format("https://steamcommunity.com/sharedfiles/filedetails/?id=", photonView.viewID, (!photonView.isSceneView) ? string.Empty : "_Value2", (!photonView.isMine) ? ("CameraFilterPack/Colors_BleachBypass" + photonView.ownerId) : "Deleted data"));
			}
		}
	}

	private void NMHIDAOMOGO()
	{
		if (InputToEvent.CAFEKBFGLKG() != null)
		{
			PhotonView photonView = InputToEvent.GHCOGMMAGHG().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 603f, (float)Screen.height - Input.mousePosition.y - 346f, 1556f, 1911f), string.Format("settings.gamemessagesduration", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : ".highscore", (!photonView.isMine) ? ("OK" + photonView.ownerId) : "_Vibrance"));
			}
		}
	}

	private void APCENGAAHMC()
	{
		if (InputToEvent.GHCOGMMAGHG() != null)
		{
			PhotonView photonView = InputToEvent.LEHGEHGANLD().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 660f, (float)Screen.height - Input.mousePosition.y - 573f, 1151f, 684f), string.Format("_Distortion", photonView.viewID, (!photonView.isSceneView) ? string.Empty : "Join failed on GameServer. Changing back to MasterServer. Msg: ", (!photonView.isMine) ? ("id" + photonView.ownerId) : "_Offsets"));
			}
		}
	}

	private void AKGHJPACNLP()
	{
		if (InputToEvent.NEGEFGKGMNG() != null)
		{
			PhotonView photonView = InputToEvent.ABPEFOBKIFI().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 577f, (float)Screen.height - Input.mousePosition.y - 1204f, 964f, 534f), string.Format("OnMastedChangeScene", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "BitsData", (!photonView.isMine) ? (" not exist" + photonView.ownerId) : "_Value2"));
			}
		}
	}

	private void ADLAFICKCFG()
	{
		if (InputToEvent.FFBJKGDNMPN() != null)
		{
			PhotonView photonView = InputToEvent.NIFAMLOPILI().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 129f, (float)Screen.height - Input.mousePosition.y - 137f, 1222f, 1697f), string.Format("\n\n#", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "masterSteamID", (!photonView.isMine) ? ("_Value" + photonView.ownerId) : "_TimeX"));
			}
		}
	}

	private void HBNHHPJNNIP()
	{
		if (InputToEvent.goPointedAt != null)
		{
			PhotonView photonView = InputToEvent.AMEAGHJJPJC().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 1081f, (float)Screen.height - Input.mousePosition.y - 915f, 1378f, 551f), string.Format(". Prefab must have a PhotonView component.", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "L1", (!photonView.isMine) ? ("_TimeX" + photonView.ownerId) : "_ScreenResolution"));
			}
		}
	}

	private void PDKFMFCMPGE()
	{
		if (InputToEvent.EPNIEGKMAHG() != null)
		{
			PhotonView photonView = InputToEvent.NLBLELFIJJC().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 668f, (float)Screen.height - Input.mousePosition.y - 1188f, 1750f, 233f), string.Format("_SmoothStart", photonView.viewID, (!photonView.isSceneView) ? string.Empty : "inventory.selected.", (!photonView.isMine) ? ("Fade" + photonView.ownerId) : "The given 2D texture "));
			}
		}
	}

	private void GGJKHODKNOC()
	{
		if (InputToEvent.LEHGEHGANLD() != null)
		{
			PhotonView photonView = InputToEvent.FFBJKGDNMPN().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 1950f, (float)Screen.height - Input.mousePosition.y - 395f, 533f, 1218f), string.Format("colorB", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "title", (!photonView.isMine) ? ("_Parameter" + photonView.ownerId) : "player.egglipszerotwo"));
			}
		}
	}

	private void LKFBADCLLML()
	{
		if (InputToEvent.NEGEFGKGMNG() != null)
		{
			PhotonView photonView = InputToEvent.GHCOGMMAGHG().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 846f, (float)Screen.height - Input.mousePosition.y - 1247f, 535f, 1542f), string.Format("DPADVER", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "_Green_G", (!photonView.isMine) ? ("CameraFilterPack/TV_Old" + photonView.ownerId) : "[PowerUp]"));
			}
		}
	}

	private void FBDOGLBMDNG()
	{
		if (InputToEvent.EPNIEGKMAHG() != null)
		{
			PhotonView photonView = InputToEvent.BGBBJJEEHGA().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 1790f, (float)Screen.height - Input.mousePosition.y - 657f, 1281f, 1459f), string.Format("[PlayerController] ", photonView.viewID, (!photonView.isSceneView) ? string.Empty : "_Intensity", (!photonView.isMine) ? ("\n" + photonView.ownerId) : "_Value"));
			}
		}
	}

	private void DKNLIFAKIKL()
	{
		if (InputToEvent.GJNOKEDHBLM() != null)
		{
			PhotonView photonView = InputToEvent.CAFEKBFGLKG().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 1403f, (float)Screen.height - Input.mousePosition.y - 1160f, 1781f, 1003f), string.Format("Connection error: ", photonView.viewID, (!photonView.isSceneView) ? string.Empty : "Scene", (!photonView.isMine) ? ("CameraFilterPack/Blend2Camera_Hue" + photonView.ownerId) : "_Value2"));
			}
		}
	}

	private void CAEGIHPNLOG()
	{
		if (InputToEvent.GJNOKEDHBLM() != null)
		{
			PhotonView photonView = InputToEvent.BHPFOCPLKKD().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 231f, (float)Screen.height - Input.mousePosition.y - 145f, 1250f, 872f), string.Format("_Value3", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "_Value2", (!photonView.isMine) ? ("Failed to InstantiateSceneObject prefab: " + photonView.ownerId) : "_DotSize"));
			}
		}
	}

	private void KOCIIOKHKME()
	{
		if (InputToEvent.LEGIHEGNHJO() != null)
		{
			PhotonView photonView = InputToEvent.NLBLELFIJJC().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 1059f, (float)Screen.height - Input.mousePosition.y - 381f, 1559f, 869f), string.Format("_Parameter", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "_ScreenResolution", (!photonView.isMine) ? ("_Blue_R" + photonView.ownerId) : "menu.tabid"));
			}
		}
	}

	private void AHAIBKMLNAP()
	{
		if (InputToEvent.BHPFOCPLKKD() != null)
		{
			PhotonView photonView = InputToEvent.EPNIEGKMAHG().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 607f, (float)Screen.height - Input.mousePosition.y - 1888f, 184f, 1065f), string.Format("_History4Weight", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "HPText", (!photonView.isMine) ? ("https://steamcdn-a.akamaihd.net/steamcommunity/public/images/clans/" + photonView.ownerId) : "MenuScene"));
			}
		}
	}

	private void JLJHPNKKJPJ()
	{
		if (InputToEvent.GJNOKEDHBLM() != null)
		{
			PhotonView photonView = InputToEvent.FFBJKGDNMPN().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 472f, (float)Screen.height - Input.mousePosition.y - 1391f, 297f, 1563f), string.Format("_Value2", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "Fade", (!photonView.isMine) ? ("PublishButton" + photonView.ownerId) : "."));
			}
		}
	}

	private void PCABPJAANFH()
	{
		if (InputToEvent.JLOKEGDMHHH() != null)
		{
			PhotonView photonView = InputToEvent.CAFEKBFGLKG().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 1944f, (float)Screen.height - Input.mousePosition.y - 1117f, 1609f, 368f), string.Format("settings.enablerankingnotifications", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "_Value4", (!photonView.isMine) ? ("_Value4" + photonView.ownerId) : "EventConfigButton"));
			}
		}
	}

	private void OKKKCIAIABC()
	{
		if (InputToEvent.GBBOGHGEMDI() != null)
		{
			PhotonView photonView = InputToEvent.GBBOGHGEMDI().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 1594f, (float)Screen.height - Input.mousePosition.y - 776f, 895f, 809f), string.Format("_Value2", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "Horizontal", (!photonView.isMine) ? ("menu.hardcoreinfo" + photonView.ownerId) : "shaders"));
			}
		}
	}

	private void PCCEPADONHK()
	{
		if (InputToEvent.NLBLELFIJJC() != null)
		{
			PhotonView photonView = InputToEvent.GBBOGHGEMDI().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 498f, (float)Screen.height - Input.mousePosition.y - 509f, 1111f, 1753f), string.Format("CameraFilterPack/Lut_TestMode", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "GameScene", (!photonView.isMine) ? (".lastCheckpoint.perfectHits" + photonView.ownerId) : "_Value5"));
			}
		}
	}

	private void PJCIENAGBGH()
	{
		if (InputToEvent.NEGEFGKGMNG() != null)
		{
			PhotonView photonView = InputToEvent.NIFAMLOPILI().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 893f, (float)Screen.height - Input.mousePosition.y - 1715f, 827f, 185f), string.Format("_EmissionColor", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "CameraFilterPack_Glasses_On4", (!photonView.isMine) ? ("No connection to item server!" + photonView.ownerId) : "inventory.selected."));
			}
		}
	}

	private void KLEHHODOCLD()
	{
		if (InputToEvent.GJNOKEDHBLM() != null)
		{
			PhotonView photonView = InputToEvent.GBBOGHGEMDI().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 663f, (float)Screen.height - Input.mousePosition.y - 884f, 684f, 1119f), string.Format("appid", photonView.viewID, (!photonView.isSceneView) ? string.Empty : "_MainTex2", (!photonView.isMine) ? ("offsets" + photonView.ownerId) : "Up"));
			}
		}
	}

	private void OJJEKKHIKAO()
	{
		if (InputToEvent.LEHGEHGANLD() != null)
		{
			PhotonView photonView = InputToEvent.AMEAGHJJPJC().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 1489f, (float)Screen.height - Input.mousePosition.y - 319f, 495f, 274f), string.Format("_Value4", photonView.viewID, (!photonView.isSceneView) ? string.Empty : "Joystick1Button11", (!photonView.isMine) ? ("SpawnObj" + photonView.ownerId) : "achievements.21.progress"));
			}
		}
	}

	private void LEKGHGJPIJE()
	{
		if (InputToEvent.NLBLELFIJJC() != null)
		{
			PhotonView photonView = InputToEvent.CAFEKBFGLKG().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 937f, (float)Screen.height - Input.mousePosition.y - 1237f, 953f, 1890f), string.Format("[LocalizationService] Loading file: ", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "Can't do manual instantiation without PhotonView component.", (!photonView.isMine) ? ("_Value2" + photonView.ownerId) : "IEnumerable"));
			}
		}
	}

	private void OLFFIKHJIEO()
	{
		if (InputToEvent.LEGIHEGNHJO() != null)
		{
			PhotonView photonView = InputToEvent.NHJCAABAOCK().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 87f, (float)Screen.height - Input.mousePosition.y - 1053f, 1096f, 1156f), string.Format("[FileSelector] Dialog canceled", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "menutheme.fragout", (!photonView.isMine) ? ("ChatHistoryInputField" + photonView.ownerId) : "ConnectionTimeout"));
			}
		}
	}

	private void DDFOMFBPFFB()
	{
		if (InputToEvent.BHPFOCPLKKD() != null)
		{
			PhotonView photonView = InputToEvent.NIFAMLOPILI().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 63f, (float)Screen.height - Input.mousePosition.y - 471f, 477f, 1805f), string.Format("_TimeX", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "(\\[ */ *quote *\\])", (!photonView.isMine) ? ("_DiffuseColor" + photonView.ownerId) : "0.000"));
			}
		}
	}

	private void KNKOCAKPJKF()
	{
		if (InputToEvent.DPPANNPKECP() != null)
		{
			PhotonView photonView = InputToEvent.GBBOGHGEMDI().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 950f, (float)Screen.height - Input.mousePosition.y - 1290f, 598f, 1135f), string.Format("menu.selectedplaymode", photonView.viewID, (!photonView.isSceneView) ? string.Empty : "_DistortionLevel", (!photonView.isMine) ? ("StartButton" + photonView.ownerId) : "[PlayerController] "));
			}
		}
	}

	private void OnGUI()
	{
		if (InputToEvent.goPointedAt != null)
		{
			PhotonView photonView = InputToEvent.goPointedAt.GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 5f, (float)Screen.height - Input.mousePosition.y - 15f, 300f, 30f), string.Format("ViewID {0} {1}{2}", photonView.viewID, (!photonView.isSceneView) ? string.Empty : "scene ", (!photonView.isMine) ? ("owner: " + photonView.ownerId) : "mine"));
			}
		}
	}

	private void JODODFGLOLD()
	{
		if (InputToEvent.GBBOGHGEMDI() != null)
		{
			PhotonView photonView = InputToEvent.DPPANNPKECP().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 1312f, (float)Screen.height - Input.mousePosition.y - 1561f, 1702f, 450f), string.Format("CameraFilterPack/Colors_Threshold", photonView.viewID, (!photonView.isSceneView) ? string.Empty : "duration", (!photonView.isMine) ? ("MenuScene" + photonView.ownerId) : "float,1.5"));
			}
		}
	}

	private void MHCDJFCKPEL()
	{
		if (InputToEvent.BGBBJJEEHGA() != null)
		{
			PhotonView photonView = InputToEvent.CAFEKBFGLKG().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 1033f, (float)Screen.height - Input.mousePosition.y - 1899f, 1988f, 649f), string.Format("_Visualize", photonView.viewID, (!photonView.isSceneView) ? string.Empty : "_ReflectionBlur", (!photonView.isMine) ? ("_FgOverlap" + photonView.ownerId) : "_Value5"));
			}
		}
	}

	private void GCLJNMOCIDM()
	{
		if (InputToEvent.NIFAMLOPILI() != null)
		{
			PhotonView photonView = InputToEvent.HNJGEGIJCIP().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 1925f, (float)Screen.height - Input.mousePosition.y - 120f, 1920f, 741f), string.Format(".status", photonView.viewID, (!photonView.isSceneView) ? string.Empty : "Set satellite beat detection sensitivity", (!photonView.isMine) ? ("colorD" + photonView.ownerId) : "id"));
			}
		}
	}

	private void AJCFIBPGAME()
	{
		if (InputToEvent.BHPFOCPLKKD() != null)
		{
			PhotonView photonView = InputToEvent.NHJCAABAOCK().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 718f, (float)Screen.height - Input.mousePosition.y - 966f, 1654f, 1024f), string.Format("EventSystem", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "_Offsets", (!photonView.isMine) ? ("Player Disconnected" + photonView.ownerId) : "_Speed"));
			}
		}
	}

	private void OGAIIBPLJBB()
	{
		if (InputToEvent.GBBOGHGEMDI() != null)
		{
			PhotonView photonView = InputToEvent.GOPJFDFJDEE().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 1577f, (float)Screen.height - Input.mousePosition.y - 1634f, 764f, 140f), string.Format("DPADHOR", photonView.viewID, (!photonView.isSceneView) ? string.Empty : "CameraFilterPack/EyesVision_2", (!photonView.isMine) ? ("Operation failed: " + photonView.ownerId) : "_LightIntensity"));
			}
		}
	}

	private void HHKCCIJMGAB()
	{
		if (InputToEvent.CAFEKBFGLKG() != null)
		{
			PhotonView photonView = InputToEvent.GHCOGMMAGHG().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 1666f, (float)Screen.height - Input.mousePosition.y - 1528f, 504f, 1198f), string.Format("/", photonView.viewID, (!photonView.isSceneView) ? string.Empty : "[Down]", (!photonView.isMine) ? ("ItemsCountText" + photonView.ownerId) : "#exit"));
			}
		}
	}

	private void LGHNKDFEOKO()
	{
		if (InputToEvent.DPPANNPKECP() != null)
		{
			PhotonView photonView = InputToEvent.LEGIHEGNHJO().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 796f, (float)Screen.height - Input.mousePosition.y - 1046f, 67f, 1314f), string.Format("Down", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "_HardBlend", (!photonView.isMine) ? ("Edited event" + photonView.ownerId) : "Edited event"));
			}
		}
	}

	private void LPKLOFJJJEI()
	{
		if (InputToEvent.DPPANNPKECP() != null)
		{
			PhotonView photonView = InputToEvent.AMEAGHJJPJC().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 503f, (float)Screen.height - Input.mousePosition.y - 446f, 108f, 1993f), string.Format("https://twitch.tv/intralism", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : " (inactive)", (!photonView.isMine) ? ("intensity" + photonView.ownerId) : "materialsquantity["));
			}
		}
	}

	private void OAGAGCMFGOM()
	{
		if (InputToEvent.BGBBJJEEHGA() != null)
		{
			PhotonView photonView = InputToEvent.ABPEFOBKIFI().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 1150f, (float)Screen.height - Input.mousePosition.y - 478f, 1610f, 267f), string.Format("PlayerBase_", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "_Value6", (!photonView.isMine) ? ("_ChangeRed" + photonView.ownerId) : "_Axis"));
			}
		}
	}

	private void OGBJNAAAEGI()
	{
		if (InputToEvent.EPNIEGKMAHG() != null)
		{
			PhotonView photonView = InputToEvent.NPIPJHKAPLE().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 122f, (float)Screen.height - Input.mousePosition.y - 759f, 1854f, 1660f), string.Format("CameraFilterPack/Blend2Camera_Lighten", photonView.viewID, (!photonView.isSceneView) ? string.Empty : "Had to lookup view that wasn't in photonViewList: ", (!photonView.isMine) ? ("NO" + photonView.ownerId) : "_Value1"));
			}
		}
	}

	private void IDDPLCAHDPO()
	{
		if (InputToEvent.NLBLELFIJJC() != null)
		{
			PhotonView photonView = InputToEvent.BHPFOCPLKKD().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 1718f, (float)Screen.height - Input.mousePosition.y - 971f, 274f, 1467f), string.Format("UseScanLineSize", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "_TimeX", (!photonView.isMine) ? ("settings_bindings_sec_" + photonView.ownerId) : "0 - sun, 1 - satellite, 2 - particles emitter"));
			}
		}
	}

	private void KCDLPGIDHCK()
	{
		if (InputToEvent.LEHGEHGANLD() != null)
		{
			PhotonView photonView = InputToEvent.AMEAGHJJPJC().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 838f, (float)Screen.height - Input.mousePosition.y - 909f, 803f, 1903f), string.Format("_Offsets", photonView.viewID, (!photonView.isSceneView) ? string.Empty : " player(s)", (!photonView.isMine) ? ("maps." + photonView.ownerId) : "Delete events"));
			}
		}
	}

	private void CPJBNBJMJFA()
	{
		if (InputToEvent.PNMKLDIOGNC() != null)
		{
			PhotonView photonView = InputToEvent.NHJCAABAOCK().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 1409f, (float)Screen.height - Input.mousePosition.y - 458f, 917f, 1988f), string.Format("#,0.00", photonView.viewID, (!photonView.isSceneView) ? string.Empty : "skin.", (!photonView.isMine) ? ("settings.gamemessagesduration" + photonView.ownerId) : "_CurrentMipLevel"));
			}
		}
	}

	private void BCODHAOJLJL()
	{
		if (InputToEvent.BHPFOCPLKKD() != null)
		{
			PhotonView photonView = InputToEvent.BPMNKAMBHFF().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 1611f, (float)Screen.height - Input.mousePosition.y - 401f, 738f, 317f), string.Format("/", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "Bad parameters for init! Use \"init\" or \"init <savename>\"", (!photonView.isMine) ? ("settings.showHP" + photonView.ownerId) : "[PlayerBase] ShowTitle error: "));
			}
		}
	}

	private void NGGLCOLOLHF()
	{
		if (InputToEvent.JLOKEGDMHHH() != null)
		{
			PhotonView photonView = InputToEvent.goPointedAt.GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 1450f, (float)Screen.height - Input.mousePosition.y - 1238f, 1817f, 1901f), string.Format("_Value3", photonView.PLMCHLCIABC(), (!photonView.isSceneView) ? string.Empty : "_TimeX", (!photonView.isMine) ? ("IconImage" + photonView.ownerId) : "maps."));
			}
		}
	}

	private void PPCJACOLHBJ()
	{
		if (InputToEvent.goPointedAt != null)
		{
			PhotonView photonView = InputToEvent.NHJCAABAOCK().GetPhotonView();
			if (photonView != null)
			{
				GUI.Label(new Rect(Input.mousePosition.x + 892f, (float)Screen.height - Input.mousePosition.y - 1772f, 1285f, 1069f), string.Format(".lastCheckpoint.time", photonView.viewID, (!photonView.isSceneView) ? string.Empty : "x", (!photonView.isMine) ? ("CameraFilterPack/Drawing_NewCellShading" + photonView.ownerId) : "y"));
			}
		}
	}
}
